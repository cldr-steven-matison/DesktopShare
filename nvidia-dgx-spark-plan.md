# NVIDIA DGX Spark — Readiness Plan (EPIC spine)

> **Status (2026-09-16).** Spine of the DGX Spark series. The prep EPIC [#226](https://github.com/cldr-steven-matison/DesktopShare/issues/226) "Prepare for Nvidia Spark DGX" is closed, as is the guide work-stream [#242](https://github.com/cldr-steven-matison/DesktopShare/issues/242) and the six field-validation issues it fathered; the work continues under EPIC [#356](https://github.com/cldr-steven-matison/DesktopShare/issues/356) — *The Complete Developer Guide for NVIDIA DGX Spark with Cloudera: root docs, field runs, authoring, public repo*. The box runs as `spark-dd06`; every work-stream issue carries `device:NvidiaSpark-1`. **Built and field-validated on the box:** the serving tier (A, #232, 2026-08-28), the day-one runbook as built, unbox to the exposed port surface plus reboot survival and the k3s-under-corp-VPN route keeper (B, #233, #322, #352, 2026-09-10/16), k3s + the Cloudera operators + the box's Kafka/`mynifi` + `SparkLlmBridge` + Flink-on-GPU with flink-agents (F, #238, 2026-08-27), the EFM agent class flow at flowVersion 10 with the cluster-side scrape green (G, #239, #324, #334), the local KB with its remote transport and the offload scoreboard (H #240, #335; L #294 closed 2026-09-10 at 7.97 % generation share, the L2 soak gate accepted as not met), the four on-box demo flows (C, #234, 2026-09-08). **Cloudera platform, field-run 2026-09-16:** CDP Base CE deployed from the box, Ozone ring fixed, reverse tunnel per NiFi worker, `Ch18LlmBridge` → Kafka (I-CE, #341/#345 — the cluster is still up: teardown [#357](https://github.com/cldr-steven-matison/DesktopShare/issues/357), clean redeploy [#358](https://github.com/cldr-steven-matison/DesktopShare/issues/358)); `srm-iceberg` live and the Iceberg REST Catalog read proven from the box with curl, the in-NiFi read open as [#355](https://github.com/cldr-steven-matison/DesktopShare/issues/355) (I-PC, #342); AWC reachability, Knox SSO, Trino/Iceberg/Ozone S3 and Kafka over OAUTHBEARER proven, Cloudera AI Inference blocked on a UI model deploy that fails on an AI Registry 503 (I-AWC, #343, [#351](https://github.com/cldr-steven-matison/DesktopShare/issues/351)); RAPIDS on GB10 and on the Cloudera AI Workbench L4 at the same 4.6× (I-AWC, #346, folded into Ch22). **Not field-run:** Cloudera AI on AWS (Ch5, Ch21) and the same-code arc (Ch24). **Blocked:** Cloudera AI on Data Services (Ch23, no test environment); multi-Spark (Ch26, hardware). The guide (J) is a 26-chapter / 9-part skeleton with 21 chapters carrying validated substance and no prose; authoring is phase 3 of #356. Purchase justification: `nvidia-request.md`.

## 1. What this is

Everything I needed *done* before the box arrived, so that arrival day was execution, not planning — and, since it landed on 2026-08-26, the spine for what runs on it. Twelve companion documents carry the detail; this file sequences them, records the decisions, and is the only place the phase gates live.

The original ask, item by item, and where each lands:

| Ask | Where it is answered |
|---|---|
| 1. Setup plans | `nvidia-dgx-spark-runbook.md` (device day-one), `nvidia-dgx-spark-k3s-cso.md` (platform), `nvidia-dgx-spark-efm-agent.md` (agent) |
| 2. Research → NVIDIA docs | `nvidia-dgx-spark-research.md` §1 NVIDIA docs + §2 playbooks (46 playbook-bucket sources — 43 official playbooks plus two forum threads and the repo index — each chapter-tagged) |
| 3. Research → DGX Spark community | `nvidia-dgx-spark-research.md` §community (NVIDIA forum category, Spark Arena, reddit) |
| 4. Research → X + bookmarked posts | `nvidia-dgx-spark-research.md` §X — the three bookmarks hydrated through the `api.fxtwitter.com` mirror, plus the similar posts found |
| 5. Research → GitHub integration examples | `nvidia-dgx-spark-research.md` §GitHub (`awesome-dgx-spark` walked, MiaAI-Lab catalogue, k8s cookbooks, monitoring, benchmarks, RAG, coding agents) |
| k3s + operators + NiFi + Kafka + Flink on the box | `nvidia-dgx-spark-k3s-cso.md` |
| EFM agent + out-of-box use cases | `nvidia-dgx-spark-efm-agent.md` |
| WindowsDesktop stays prod; GPU services as-is until Spark equivalents | `nvidia-dgx-spark-k3s-cso.md` §cutover ladder |
| Local AI knowledge base for Claude Code | `nvidia-dgx-spark-local-kb.md`, `nvidia-dgx-spark-remote-kb.md`, `nvidia-dgx-spark-offload.md` |
| NVIDIA ↔ Cloudera — two scopes: **platform** (CDP Base CE, CDP Public Cloud, Cloudera AWC on AWS) and **Cloudera AI** (on AWS, on AWC, on Data Services) | `nvidia-dgx-spark-cloudera-aws.md` + `cloudera-ce-aws-runbook.md` (Base CE), `nvidia-dgx-spark-cloudera-awc.md` + `cloudera-anywhere-getting-started.md` (AWC), `nvidia-dgx-spark-rapids-runbook.md` (RAPIDS, under Cloudera AI on AWC); Data Services has no source doc yet (blocked, no test env) |
| The guide | `Complete Developer Guide for Nvidia Spark with Cloudera.md` (tracker) + `files/nvidia-spark-guide/` (skeleton, 26 chapters in 9 parts) |

## 2. The box

GB10 Grace Blackwell (20-core Arm, aarch64), **128 GB LPDDR5x unified @ 273 GB/s**, ~1 PFLOP FP4, 4 TB NVMe, ConnectX-7 200 Gb/s, ~240 W ([NVIDIA](https://www.nvidia.com/en-us/products/workstations/dgx-spark/)). Ships with DGX OS (Ubuntu-based) and the NVIDIA container runtime. The consequence that shapes every choice below: capacity holds ~100–200 B models, bandwidth caps decode speed, so the interactive workhorse is a ~20–35 B model at NVFP4 and the ~100 B class is a single-user showpiece. Full sizing in `nvidia-dgx-spark-landscape.md`.

**Naming rule, repo-wide:** "DGX Spark" (or "the Spark box") is the hardware; "Apache Spark" is the engine RAPIDS accelerates. Never bare "Spark" in a Cloudera-integration sentence — recorded in `CONTEXT.md`.

## 3. Scope boundaries

- **The Spark is not a second production cluster.** WindowsDesktop's `cld-streaming` minikube stays the prod CSO host (NiFi, EFM, Kafka, the Streamers app, the Telegram bridge). The Spark runs its *own* k3s + operators as a development and demo platform, and is an inference target for flows on any device.
- **GPU services migrate one rung at a time, never in a batch.** WindowsDesktop's vLLM (`:8000`, Qwen2.5-3B — also the OpenClaw bridge's model), Whisper (`:8001`), TEI embeddings, and the Jetson-style `trt-infer` daemon keep running as-is. Each moves only when the Spark equivalent is up, load-tested from another device, and has a rollback (`nvidia-dgx-spark-k3s-cso.md` §cutover ladder). The OpenClaw bridge's `127.0.0.1:8000` dependency is the one that must not break silently.
- **Not the whole CSO stack.** Schema Registry, Surveyor, SSB and the monitoring stack are optional on the Spark; NiFi, Kafka (Strimzi) and Flink are required. What's in and out per namespace is in `nvidia-dgx-spark-k3s-cso.md` §budget.
- **The box is a client of every Cloudera environment, never a node.** CE Base, CDP Public Cloud and AWC are reached over a tunnel, a Knox gateway or the corp VPN; nothing on AWS runs on the box, and nothing on the box is a cluster member.
- **Every deploy, redeploy or teardown of a Cloudera environment is an explicit go from Steven in that turn, with the cost line stated first.** An issue body is never a go (`agent/incident-rules.md` §"Unauthorized infra mutation", #344).

## 4. Work-stream table

Sub-issue titles follow the Close-Plan-v2 convention (`DGX Spark · Letter — scope`). Since 2026-08-26 every issue in the series carries `device:NvidiaSpark-1`; Mac-side items carry `device:FTF3XR2065` as well.

| WS | Scope | Doc | Issue | State (2026-09-16) |
|---|---|---|---|---|
| A | Capability landscape & model sizing | `nvidia-dgx-spark-landscape.md` | [#232](https://github.com/cldr-steven-matison/DesktopShare/issues/232) | **DONE, closed 2026-08-28 (`67cb1da`/`e50e471`)** — MoE-vs-dense (§2.5), serving-engine table (§3.5), 128 GB co-host budget (§5.5), 3-candidate model lock (§6); full serving tier locked + measured on `spark-dd06`; Phase-0 model-lock gate closed. Feeds Ch1, Ch4, Ch6, Ch26 |
| B | Day-one device runbook | `nvidia-dgx-spark-runbook.md` | [#233](https://github.com/cldr-steven-matison/DesktopShare/issues/233), [#322](https://github.com/cldr-steven-matison/DesktopShare/issues/322), [#352](https://github.com/cldr-steven-matison/DesktopShare/issues/352) | **DONE, all three closed.** The as-built narrative from `spark-dd06`: baseline, network, `:8000` and the co-hosted tiers, the stretch swap-in, k3s + operators, roster, the exposed port surface, hardening (#233, 2026-09-10; ufw corrected the same day, `files/issue-233/ufw-nodeports.sh`); reboot survival owned by systemd, NVMe APST off on the kernel command line (#322, 2026-09-10/12); the k3s `node-ip` pin per Wi-Fi profile and `k3s-vpn-route.timer` keeping the service CIDR off the corp VPN (#352, 2026-09-16, `files/issue-352/`). Still owed: the router reservation and the 10 GbE port. Feeds Ch2, Ch3 |
| C | CSO Operators / DGX Demo Designs | `nvidia-dgx-spark-cso-demos.md` + `nvidia-dgx-spark-rapids-demo.md` | [#234](https://github.com/cldr-steven-matison/DesktopShare/issues/234), [#353](https://github.com/cldr-steven-matison/DesktopShare/issues/353) | **DONE, closed.** The four flows built with EFM + NiFi on the box (EFM front door, SparkLlmBridge, ReleaseVoteWatch + TelegramNotify, flink-agents), each with a live export (#234, 2026-09-08), plus the RAPIDS → Cloudera AI on AWC live-demo runsheet (#353, 2026-09-16). Feeds Ch25 |
| D | On-box bring-up | (executed B, then F §1, G §1) | [#235](https://github.com/cldr-steven-matison/DesktopShare/issues/235) | **DONE, closed** — box landed 2026-08-26 as `spark-dd06`; Phase 3 gate closed 2026-09-10 |
| E | Research corpus — NVIDIA docs, playbooks, community, X, GitHub, Cloudera AI/AWS | `nvidia-dgx-spark-research.md` | [#237](https://github.com/cldr-steven-matison/DesktopShare/issues/237) | **DONE, closed** — 653 lines, 241 sourced URLs, 12 numbered sections the siblings cite |
| F | k3s + CSO operators (NiFi/Kafka/Flink) on aarch64; GPU-services cutover ladder | `nvidia-dgx-spark-k3s-cso.md` | [#238](https://github.com/cldr-steven-matison/DesktopShare/issues/238) | **DONE, closed (Phase 4, 2026-08-27)** — §§4, 5, 6 and 8 as built: operators + ingress-nginx with ssl-passthrough, the box's own Kafka and `mynifi`, the `SparkLlmBridge` gate flow, Flink claiming the GPU, flink-agents 0.3.1 `STABLE`. The §9 cutover ladder is planning-only by decision. `cfm-operator` ≥ 3.3.x is amd64-only (#338); the box stays on the pinned release. Feeds Ch7–Ch11 |
| G | EFM agent class `NvidiaSpark-1` + out-of-box use cases | `nvidia-dgx-spark-efm-agent.md` | [#239](https://github.com/cldr-steven-matison/DesktopShare/issues/239), [#324](https://github.com/cldr-steven-matison/DesktopShare/issues/324), [#334](https://github.com/cldr-steven-matison/DesktopShare/issues/334) | **DONE, closed 2026-09-10/14.** Single-handler router at flowVersion 10 (`:8190` fronts `/reason`, `/embed`, `/rerank`, `/transcribe` and the `/v1/*` OpenAI aliases), export `files/issue-226/flows/NvidiaSpark-1.designer-flow.json`; fleet Prometheus scrapes `:9936` and `:9835` at `up=1` over the tailnet with a fleet-board row; use cases 1 and 3 answered from non-Spark devices; source doc's editorial pass done. Feeds Ch12–Ch14 |
| H | Local knowledge base + local agentic validation for Claude Code | `nvidia-dgx-spark-local-kb.md`, `nvidia-dgx-spark-remote-kb.md` | [#240](https://github.com/cldr-steven-matison/DesktopShare/issues/240), [#335](https://github.com/cldr-steven-matison/DesktopShare/issues/335) | **DONE, closed.** Retrieval (`96ec9bf`: Qdrant+TEI, `desktopshare-kb`, `ds-kb` MCP), validator (`a11cad4`: `validator.py`, guard rule 10.5 advisory), the remote transport `kb-remote.service` on `:8191` (#335). **Carried into #356:** the H5 soak has no verdict because the validator writes no ledger — instrumentation first. Feeds Ch15–Ch17 |
| I-CE | CDP Base CE on AWS + the DGX Spark | `nvidia-dgx-spark-cloudera-aws.md` §2, `cloudera-ce-aws-runbook.md` | [#341](https://github.com/cldr-steven-matison/DesktopShare/issues/341), [#345](https://github.com/cldr-steven-matison/DesktopShare/issues/345) → [#357](https://github.com/cldr-steven-matison/DesktopShare/issues/357), [#358](https://github.com/cldr-steven-matison/DesktopShare/issues/358) | **Field-run 2026-09-16, #341/#345 closed.** CE Base deployed from the box on the arm64-native EE (3 h 40 min, ~$2/h, 11 nodes); Ozone SCM ring fixed with `hdds.grpc.tls.enabled=false` marked `final`; reverse tunnel per NiFi worker; `Ch18LlmBridge` PG → Kafka over SASL_SSL/GSSAPI, read back; worker sizing `r5a.2xlarge` recorded. **The 09-16 cluster is still running** (Steven's call): teardown + three zeros is #357, the clean end-to-end re-run of the runbook is #358 — both explicit-go. Feeds Ch18 |
| I-PC | CDP Public Cloud on AWS + the DGX Spark | `nvidia-dgx-spark-cloudera-aws.md` §3 | [#342](https://github.com/cldr-steven-matison/DesktopShare/issues/342) → [#355](https://github.com/cldr-steven-matison/DesktopShare/issues/355) | **Tier A proven 2026-09-16, #342 closed.** `srm-iceberg` fully live (env / Data Lake / Impala Data Hub / Trino VW); the Iceberg REST Catalog read path proven from the box with curl (Knox OAuth2 → namespaces/tables → `loadTable` + vended S3 STS creds; a temporary Knox-SG `/32` for the box's VPN egress, revoked after). The in-NiFi read (`InvokeHTTP` + OAuth2, `GetIceberg`, `QueryIceberg`, NAR built + deployed into the box's CFM NiFi) is #355 (in progress); the VPN↔k3s conflict that blocked it is fixed (#352). CDF DataFlow and Cloudera AI Inference are not deployed on the env. Feeds Ch19 |
| I-AWC | Cloudera AWC on AWS + Cloudera AI on AWC (incl. RAPIDS) | `nvidia-dgx-spark-cloudera-awc.md`, `cloudera-anywhere-getting-started.md`, `nvidia-dgx-spark-rapids-runbook.md` | [#283](https://github.com/cldr-steven-matison/DesktopShare/issues/283), [#343](https://github.com/cldr-steven-matison/DesktopShare/issues/343), [#346](https://github.com/cldr-steven-matison/DesktopShare/issues/346), [#351](https://github.com/cldr-steven-matison/DesktopShare/issues/351), [#284](https://github.com/cldr-steven-matison/DesktopShare/issues/284) | **Platform proven 2026-09-16, #343/#346 closed.** Reachability over the corp VPN (#347), CA chain, Knox SSO, CDF/SSB/Trino/Iceberg/Ozone S3 proven; Kafka produce/consume proven over SASL_SSL/OAUTHBEARER with an `access-keys/token` client_credentials access-token (#351; the broker's only mechanism, `hadoop-jwt` fails for lacking `exp`). RAPIDS: cuDF/cuML on GB10 (4.6×, 131× strings) and the same `cudf_bench.py` on the Workbench L4 at 4.6×; Spark RAPIDS plugin runs on arm64; CDE has no GPU node group. **Open:** Cloudera AI Inference — auth solved, no model endpoint, the UI deploy fails on an AI Registry 503 (#351, Mac, blocked); the Trino catalog add needs a GOES admin (#284, Mac, blocked). Feeds Ch20, Ch22, Ch24 |
| I-AI | Cloudera AI on AWS; Cloudera AI on Data Services; the same-code arc | `nvidia-dgx-spark-cloudera-aws.md` §3.4, §5 | [#241](https://github.com/cldr-steven-matison/DesktopShare/issues/241) (design, closed) → [#356](https://github.com/cldr-steven-matison/DesktopShare/issues/356) phase 2 | **Design-accepted, not field-run.** Ch5/Ch21 (Workbench → AI Registry → AI Inference on AWS, NIM, the base-URL swap) need a run or an explicit design-only decision; Ch24's arc records end to end once one Cloudera AI Inference endpoint answers (Ch21 or Ch22). Ch23 (Data Services) has no source doc and no test environment |
| J | The guide — tracker + 26-chapter skeleton | `Complete Developer Guide for Nvidia Spark with Cloudera.md`, `files/nvidia-spark-guide/` | [#242](https://github.com/cldr-steven-matison/DesktopShare/issues/242) (closed) → [#356](https://github.com/cldr-steven-matison/DesktopShare/issues/356) | **26 chapters / 9 parts, 26/26 stubs on the task-first template, 21/26 with validated substance, 0 prose.** RAPIDS folded into Ch22 (2026-09-16); ch18/19/20 returned to stubs with their as-built content in the source docs. Authoring is #356 phase 3; the public repo is phase 4. Artifact placement (scripts, exports, patches under `files/issue-*/`) is [#359](https://github.com/cldr-steven-matison/DesktopShare/issues/359) from `files/issue-242/artifact-inventory.md` |
| K | Verify CSO image architecture on-box (aarch64) | (comment on the issue) | [#243](https://github.com/cldr-steven-matison/DesktopShare/issues/243) | **DONE, closed** — all arm64 on the registry; on-box confirmed by F |
| L | Local-inference offload — the standing offload-ratio scoreboard | `nvidia-dgx-spark-offload.md` | [#294](https://github.com/cldr-steven-matison/DesktopShare/issues/294) | **Closed 2026-09-10 (accepted).** Ledger row 12: generation offload ratio **7.97 %** (296,934 box ÷ 3,429,789 Claude output tokens, 149 sessions), KB adoption 9.4 % — the L2 soak gate (≥ 50 %) accepted as not met; `offload.py`, `kb-retrieve.sh` and `compress.py` keep running. L1–L3 built, L4 measured (per-section extraction at parity with the hosted pair). Feeds Ch16, Ch17 |

## 5. Phased build plan

Each phase has a gate that must be true before the next starts.

### Phase 0 — Gates (closed)

| Gate | Decided by | State |
|---|---|---|
| **Model lock** — lead (~27–35 B NVFP4 interactive) and stretch (~100 B) demo drivers, plus the embedding/rerank/Whisper set | **Full set locked 2026-08-28 (#232):** lead `nvidia/Qwen3.6-35B-A3B-NVFP4` (`:8000`), stretch `nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4` (`:8000` swap-in), embed `BAAI/bge-m3` (`:8001`), rerank `BAAI/bge-reranker-v2-m3` (`:8002`), STT whisper.cpp `large-v3` (`:8003`) — three sourced candidates per slot in landscape §6, serve scripts in `files/issue-226/` | **closed** |
| **CSO on aarch64** | All 16 Cloudera images are multi-arch with `linux/arm64` (`nvidia-dgx-spark-research.md` §9); confirmed on the box under F. The one exception found since: `cfm-operator` ≥ 3.3.x is amd64-only (#338) | **closed — arm64 native** |
| **Kubernetes substrate** | k3s on the host, pinned v1.32.13+k3s1 under the CSA/CSM 1.32 ceiling (`nvidia-dgx-spark-k3s-cso.md` §3) | **closed 2026-08-27** |
| **Guide repo** | skeleton staged in `files/nvidia-spark-guide/`; public repo cut in #356 phase 4, after the chapters are written | decided 2026-08-24, re-sequenced 2026-09-16 |
| **X bookmarks** | the three links on #226 are the bookmarks; research hydrates them via the fxtwitter mirror — no OAuth2 work | decided 2026-08-24 |

### Phase 1 — Research corpus (E) — closed

`nvidia-dgx-spark-research.md`: every claim the other docs make traces to an entry here, and every entry says which chapter it feeds. Load-bearing claims carry a three-lens adversarial verification tag.

### Phase 2 — Plans (A–C expanded, F–J new) — closed

Every doc in §4 exists at full depth, opens with the Status header, closes with Definition of done / When this ships / Resources, and cross-links resolve.

### Phase 3 — Arrival day (D) — closed 2026-09-10

Runbook B top to bottom, then G §1. **Gate:** another device's `curl http://<spark>:<port>/v1/models` answers, and EFM shows `NvidiaSpark-1` online — met: WindowsDesktop's flows have called `:8000` daily since 08-27, the class flow answered a `POST /reason` from a WindowsDesktop shell, the fleet Prometheus scrapes both exporters green (#324). Off-box callers use the tailnet `100.104.155.57`, not `192.168.1.203`.

### Phase 4 — Platform (F) — closed 2026-08-27

k3s with the GPU device plugin, the operators in the canonical order (cert-manager → Strimzi → CSA → CFM), NiFi → local-LLM flows, Flink on GPU. **Gate:** a NiFi flow on the Spark's own cluster lands an LLM response into the Spark's own Kafka — met by the `SparkLlmBridge` PG (`nvidia-dgx-spark-k3s-cso.md` §6, export `files/issue-226/flows/SparkLlmBridge.json`). The cutover ladder is planning-only by decision (§6, 2026-08-27).

### Phase 5 — Integrations (H, L, C, I) — closed for what was run; the remainder is #356 phase 2

Local knowledge base answering Claude Code queries over our own docs (H, built); the local validator measured (L, closed with the L2 gate unmet); the demo catalogue (C, closed); the Cloudera integrations run against real environments. **What ran (2026-09-16):** CE Base end to end from the box (I-CE), the REST Catalog read from the box on CDP Public Cloud (I-PC, Tier A), the AWC platform incl. Kafka and RAPIDS on the Workbench L4 (I-AWC). **What did not:** the in-NiFi Iceberg read (#355), Cloudera AI Inference on AWC (#351, AI Registry 503), Cloudera AI on AWS (Ch5/Ch21), the same-code arc (Ch24) — the original gate, "same code, N backends recorded end-to-end", is not met and moves to #356 phase 2.

### Phase 6 — The guide (J) → EPIC #356

The guide's own phases, gated in order: **(1) root docs** — every source doc current, tidy and self-contained, issue-dir findings folded in (closed 2026-09-16 by the #242 pass); **(2) remaining field runs and blockers** — #355, #351, #284, #180, #357, #358, #359, Ch5/Ch21/Ch24 run or decided design-only, plus the carried decisions (H5 ledger, Ch23 environment, Ch26 hardware); **(3) authoring** — 26 chapters in part order from the source doc named in the tracker row, inside the blog-voice band, provenance stripped, one commit per chapter; **(4) public repo** — cut from `files/nvidia-spark-guide/`, README as the front door, source docs to `completed/` as each chapter lands. No authoring before phase 2 closes.

## 6. Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-09-16 | **RAPIDS is not a chapter. It folds into Ch22 (Cloudera AI on AWC)**; the runbook stays a root doc as Ch22's second source, the live-demo runsheet is Ch25's fifth demo, the tracker's numberless RAPIDS row is gone. | Steven's call on #242: the AWC Workbench is the one Cloudera AI surface the box has run against, so the RAPIDS exercise belongs there, not in a chapter of its own. |
| 2026-09-16 | **No chapter authoring until #356 phase 2 closes; ch18, ch19 and ch20 returned to the stub template.** Their as-built content lives in the source docs (`nvidia-dgx-spark-cloudera-aws.md` §2.5/§3.6, `nvidia-dgx-spark-cloudera-awc.md`, `cloudera-ce-aws-runbook.md`). | Steven's call on #242 ("no authoring, remove ch. 20 authoring"); the three drafts also sat outside the blog-voice band and carried provenance. Root docs first, then prose from a stable base. |
| 2026-09-16 | **#242 and its six field-validation children close together; EPIC #356 replaces them, clean.** Successors: #355 (in-NiFi Iceberg read, from #342), #357 (CE teardown, from #345), #358 (CE clean redeploy, from #341), #359 (artifact homes, from #242), #351 (CAI Inference, from #343). | Steven's rule on #242: evaluate the whole guide for correctness first, then close everything and start the new EPIC free of the old threads. The evaluation record is `files/issue-242/eval-2026-09-16.md`. |
| 2026-09-16 | **The 09-16 CE Base cluster stays running until #357 runs it down, on an explicit go.** ~$2/h, 11 nodes. | Steven's call at the end of the #345 session ("keep it") and again on the #242 pass ("file the tasks, leave the cluster running"). Guard 17 asks at the command; the cost line goes first. |
| 2026-09-16 | **AWC Kafka is reached with an OAuth2 `client_credentials` access-token from `POST /api/v0/auth/access-keys/token`, never the `hadoop-jwt` cookie.** The same token is the Cloudera AI Inference credential once an endpoint exists. | #351, live from the Mac: OAUTHBEARER is the broker's only enabled mechanism; the SSO cookie carries no `exp` claim and is rejected with "Expiry not set"; the minted token authenticates, and produce→consume round-trips. #343's "needs platform-admin SASL credentials" was wrong. |
| 2026-09-16 | **Ch19's in-NiFi read is its own issue (#355), not a reopen of #342.** | The Tier-A curl proof closes what #342 could prove from the box; the NiFi build (`InvokeHTTP` + OAuth2 CS, `GetIceberg`/`QueryIceberg` NAR) is a build task with its own hand-off (`files/issue-342/nifi-tier-b-plan-2026-09-16.md`). |
| 2026-09-12 | **The disk is part of reboot survival (#322 re-opened).** `nvme_core.default_ps_max_latency_us=0 pcie_aspm=off` on the kernel command line through a GRUB drop-in that `install.sh` owns (step 0). | Seven boots wedged in 56–97 s on 2026-09-12; the first boot on the flag ran clean. A box that answers ping but blocks every process on `nvme0n1` is a storage fault. |
| 2026-09-10 | **#241 (I) accepted and closed as design-only; #294 (L) accepted and closed with the L2 soak gate unmet.** | Steven's call: the design and fact-check work on I was complete and the field run gated on AWS access; L's remaining gate needed weeks of organic adoption, and 9.4 % against 50 % after a week is the finding. |
| 2026-09-10 | **Reboot survival is owned by systemd with one source of truth per container (#322).** `nvidia-serve-boot.service` waits for the GPU and re-runs each container's committed serve script; `minifi-java` is a native unit; a post-boot verifier posts its report. | The 2026-09-08 reboot left four GPU containers `Exited (128)` and the caption pipeline down for two days (#321). |
| 2026-09-10 | **The H5 validator soak cannot be judged: no firing was ever logged.** The call is deferred until a ledger exists (carried into #356). | A pass or fail on a gate with zero recorded data would be a guess dressed as a decision. |
| 2026-09-09 | **Guide split into two Cloudera scopes; grew to 26 chapters / 9 parts (#242).** Part VI is the platform (CDP Base CE / CDP Public Cloud / AWC); Part VII is Cloudera AI (on AWS / on AWC / on Data Services) + the same-code arc. Stubs on a task-first template that does not mirror the EFM guide. | Steven's review pass: using the box with each platform form factor is a different scope from Cloudera AI's own form factors, and Data Services is the on-prem edge case. |
| 2026-09-08 | **DGX demos are what was built with EFM and NiFi on this box, DGX-track only.** | Steven's call on the review pass: a demo catalogue that names other devices' artifacts is not a DGX Spark catalogue. |
| 2026-09-02 | **Work-stream L filed (#294): execute §5 of `nvidia-dgx-spark-local-kb.md` as a standing offload program.** | Steven's call: "we need to work on how we can further reduce *Claude does essentially all of it*." Measured the same day: 1.50 % generation share, `kb_search` invoked in 2 of 101 sessions. |
| 2026-08-28 | **Model lock closed (#232):** lead `nvidia/Qwen3.6-35B-A3B-NVFP4` (`:8000`), stretch `nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-NVFP4` (swap-in), embed `BAAI/bge-m3` (`:8001`), rerank `BAAI/bge-reranker-v2-m3` (`:8002`), STT whisper.cpp `large-v3` (`:8003`). | Completes Phase 0. All NVFP4 MoE for the generation tiers by the active-parameter argument (landscape §2.5); TEI sm_121 for embed/rerank; whisper.cpp `120;121` for STT. |
| 2026-08-27 | **No GPU-service cutover in the foreseeable future.** §9's ladder is a planning deliverable; WindowsDesktop keeps every GPU service it runs today. | The box has its own k3s, Kafka, NiFi and endpoint; cutting over would trade a working prod for no new capability, and the OpenClaw bridge's `127.0.0.1:8000` dependency is a live risk with no upside. |
| 2026-08-27 | The NiFi UI's Ingress route is served by **ingress-nginx installed with `--enable-ssl-passthrough`**, host-network on the box's own `:443`. | k3s runs with `--disable traefik`; keeping Ingress means `files/cso-prod-1/nifi-cso-prod-1.yaml` drops in unchanged; the passthrough flag is exactly what minikube's addon omits (#254). |
| 2026-08-27 | The box's Kafka external listener uses its **own** NodePort block — `32100` bootstrap, `32101–32103` brokers, `advertisedHost` = the box's LAN IP. | Prod's `31623/31850/31935/30336` stay prod's; a client on WindowsDesktop talks to both clusters. |
| 2026-08-27 | SSB is **not** installed with the CSA operator on this box (`ssb.enabled=false`). | §5's budget makes SSB demo-time, not resident. |
| 2026-08-27 | k3s on the host is the Kubernetes substrate; "k3d" in the original ask was a typo. | Every first-hand report of Kubernetes with GPU on a GB10 runs k3s; it auto-detects the NVIDIA runtime and its NodePorts are host ports. |
| 2026-08-26 | Ownership of #226 and A–K → `NvidiaSpark-1`; K re-scoped on-box; D unblocked. | The box landed and runs its own session. |
| 2026-08-24 | Device name `NvidiaSpark-1`, label `device:NvidiaSpark-1`; guide skeleton staged in DesktopShare, repo later; X research via the `api.fxtwitter.com` mirror; NIM is the serving shape for anything that must match Cloudera AI Inference, SGLang/vLLM for raw speed. | The pre-arrival package: names, labels and glossary before anything had to be renamed; a repo of stubs is noise; the mirror returns full post JSON; API parity is the demo payload, speed is the booth payload. |

## 7. Risk register

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| **The 09-16 CE Base cluster bills while it waits for its teardown** | ~$2/h, 11 EC2 nodes in `us-east-2`, running since 2026-09-16 02:32 UTC | #357 (explicit-go teardown with the three-zero proof); the runbook §0 contract says tear down or pause in the same session unless told to keep it | #357 |
| **goes01 AI Registry returns 503** | No Cloudera AI Inference endpoint can be deployed on AWC, so Ch22's inference half, Ch24's arc and the NIM-parity demo cannot run | Platform-admin fix on the GOES side; the recovery probe and the retry sequence are in `nvidia-dgx-spark-cloudera-awc.md` §3 | #351 |
| Two agent sessions share the box's checkout | A concurrent session can revert a file mid-run (happened 2026-09-16 during the CE runbook) | Explicit-path `git add`, `git status` before every commit, `git pull --ff-only` before every push | every session |
| `cfm-operator` ≥ 3.3.x is amd64-only | A CFM upgrade on the box crash-loops the operator | Stay on the pinned release; `CLAUDE-CHECKIN.md` records the ceiling (#338) | F |
| k3s cannot see the GPU on GB10 | Ch8 blocks Ch9–11 | Device plugin ≥ v0.17.4 (UMA fix); proven on the box | F |
| 128 GB is shared by the LLM and the k3s cluster | A ~100 B model at ~94 % memory leaves nothing for NiFi/Kafka/Flink | `nvidia-dgx-spark-k3s-cso.md` §5 (measured resident set against 121 GB usable); the stretch model is a demo mode, not a resident | F, A |
| Migrating vLLM `:8000` breaks the OpenClaw Telegram bridge | Phone replies fail silently (#192 lesson) | No cutover (decision 2026-08-27); the bridge's model stays on WindowsDesktop | F |
| CE / CDP Base images are amd64-only | Base cannot run *on* the Spark — irrelevant, Base runs on AWS; the demo must not promise otherwise | Stated in `nvidia-dgx-spark-cloudera-aws.md` | I |
| Community numbers are stale (the field moves monthly) | Model lock made on superseded data | Every number dated; the box's own measurements win | E, A |
| The guide drifts from the tracker | Stale-spec trap (the EFM guide paid for this under #111; this guide paid for it on 2026-09-16 with a plan doc three issues behind) | Touch an asset, sweep every linked surface in the same pass: tracker row, plan row, README, stub status line | J |
| A reboot leaves the Docker serving tier down | Fired once, 2026-09-08 (#321) | `nvidia-serve-boot.service` + `nvidia-post-boot-verify.service` (#322, `files/issue-322/`) | #322 |
| The NVMe controller hangs in a low-power state | Fired 2026-09-12: seven boots froze | `files/issue-322/nvme-apst-off.cfg` on the kernel command line; runbook §8 signature | #322 |
| The corp VPN swallows the k3s service CIDR | NiFi on the box crash-loops whenever the VPN is up | `k3s-vpn-route.timer` (`files/issue-352/`) keeps `10.43.0.0/16` off `tun0`; the `node-ip` pin has to be moved by hand on the other Wi-Fi profile (runbook §2) | #352 |

## 8. How this revision was built

Thirteen first-pass research agents (`r01`–`r13`), a completeness critic over the merged corpus, a four-bucket gap round (`g01`–`g04`), and a three-lens refute pass on the load-bearing claims — then one author per document, each followed by a lint pass for sources, style, and cross-links. Research and lint ran on the cheaper model tier; authoring on the mid tier; only orchestration and synthesis on the top tier. The five docs E–I were authored on the box on 2026-08-26 by `files/issue-226/authoring-workflow.js` v2. The 2026-09-16 correctness pass (#242) ran four read-only evaluation agents over the fourteen source docs and the 26 stubs and applied their punch lists; the record is `files/issue-242/eval-2026-09-16.md`.

## Open questions

- Whether Cloudera AI Inference on-prem has moved from Technical Preview to GA in 2026 — research §Cloudera AI carries the current answer and its date; it decides whether Ch23 can ever get a test environment.
- A second DGX Spark: the dual-box recipes (1M context over ConnectX-7) scale with no software rework — a hardware decision, not a plan item (Ch26).
- Whether Cloudera AI on AWS (Ch5, Ch21) gets a field run or stays design-only — decided in #356 phase 2.

## Definition of done (this revision)

- Every source doc in §4 opens with a Status header that matches its body and GitHub, and every cross-link resolves.
- The tracker, this plan, the guide README and the 26 stub status lines agree: 26 chapters, 9 parts, 21 with validated substance, 26/26 stubs, 0 prose, EPIC #356.
- Every as-built finding that lived only under `files/issue-*/` is in its root doc; the inventory of the rest is `files/issue-242/artifact-inventory.md`.
- #242, #341, #342, #343, #345, #346 closed with the evaluation; #356 open with its children.

## When this ships

- #356 phase 2 runs the remaining field work on explicit go: #357 → #358 (CE), #355 (in-NiFi Iceberg read), #351 once the goes01 registry is healed, #284, #180, #359.
- Any doc here that changes a canonical flow shape gets recorded back into the `nifi-and-ai` skill.
- Chapters are authored in part order in phase 3; customer-facing demos get clean blogs per `agent/writing-style.md`.

## Resources

- Companion docs: `nvidia-dgx-spark-research.md` · `nvidia-dgx-spark-landscape.md` · `nvidia-dgx-spark-runbook.md` · `nvidia-dgx-spark-k3s-cso.md` · `nvidia-dgx-spark-efm-agent.md` · `nvidia-dgx-spark-local-kb.md` · `nvidia-dgx-spark-remote-kb.md` · `nvidia-dgx-spark-offload.md` · `nvidia-dgx-spark-cloudera-aws.md` · `cloudera-ce-aws-runbook.md` · `nvidia-dgx-spark-cloudera-awc.md` · `cloudera-anywhere-getting-started.md` · `nvidia-dgx-spark-rapids-runbook.md` · `nvidia-dgx-spark-rapids-demo.md` · `nvidia-dgx-spark-cso-demos.md` · `Complete Developer Guide for Nvidia Spark with Cloudera.md` · `files/nvidia-spark-guide/README.md`
- Precedent: `efm-nvidia-nano-research.md` / `efm-nvidia-nano-inference.md` / `efm-nvidia-jetson-nano.md` (the Jetson onboarding shape) · `flink-plan.md` + `flink-agents-cso-plan.md` · `cloudera-iceberg-rest-catalog-aws-plan.md` · `cloudera-iceberg-rest-catalog-aws-plan-redeploy.md` · `blog/cloudera-ce-cm-evaluation.md` · `cso-operator-app-plan.md`
- Purchase justification: `nvidia-request.md`
- [DGX Spark specs](https://www.nvidia.com/en-us/products/workstations/dgx-spark/) · [DGX Spark User Guide](https://docs.nvidia.com/dgx/dgx-spark/) · [NVIDIA/dgx-spark-playbooks](https://github.com/NVIDIA/dgx-spark-playbooks) · [Cloudera + NVIDIA](https://www.cloudera.com/partners/solutions/nvidia.html)
