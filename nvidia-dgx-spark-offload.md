# Moving Claude Code's mechanical work onto the DGX Spark, and keeping score

> **Status (2026-09-03 — L3 built, gate met):** `files/issue-226/kb/compress.py` is the context-compression pre-pass from §5's "log triage" row — a dump is summarized by the box's model and only the conclusion crosses into hosted context, raw error lines pasted verbatim. Two real dumps, both rows in `offload-workloads.jsonl`: the healthy `mynifi-0` stdout (19,887 tokens → 275 in 3.4 s, correct verdict, both real warnings found — my own `grep ' WARN '` had missed them) and the box's **crash-looping Kafka broker's 1.28 MB `--previous` log** (429,745 tokens → 1,009 in 170 s over 22 chunks; the fencing/election timeline and the `DuplicateBrokerRegistrationException` found with raw lines). **Hosted input avoided ≈ 448 K tokens across the two runs.** Two limits measured, not assumed: log-only triage cannot see an external kill (`OOMKilled exit 137` is in the pod status, not the log), and the `NEXT` command is the mechanical tier's weak spot (§3, §5). The call-site offer for `kubectl logs` is the follow-on. **L4 measured the same day, gate not met — and that is the finding.** `measure.py` ran lint and extract on this doc with Claude adjudicating: lint returned 13 findings and **0 were actionable** (ten apply blog-post rules to a plan doc, one is a hallucination — "fences lack a language tag" when every fence is tagged); extract's facts were **13 of 15 sampled correct**, but it produced 249 claims against a budget of 60 with 48 near-duplicate pairs, after truncating twice at 2,500 and 8,000 output tokens unbounded. A deterministic dedupe was built the same day and measured: safe and small — it removes 7–10 %, and even tightened about 1 in 4 of its drops is a distinct short fact — while the volume problem was atomization at 4–5× the budget. **Per-section extraction fixed that:** 10 of 10 sections within budget, 70 claims for 313, 22 s for 63, shards 12 % for 43 %, and **15 of 15 sampled correct** — extract moves as the first pass with Claude curating. A plan-doc rulebook rebuilt the lint prompt: on a doc with real violations it recovers 3 of the 4 hosted findings at 5 of 8 real; on a clean doc it fills one finding per rule and cannot say `clean` — a pre-screen, adjudication mandatory, not moved. §5.1's verdict, with numbers: the mechanical tier's *facts* move; its *judgment and volume control* stay hosted (§3, §5).
>
> **Status (2026-09-02 — L1 built):** work-stream **L** of EPIC [#226](https://github.com/cldr-steven-matison/DesktopShare/issues/226), issue [#294](https://github.com/cldr-steven-matison/DesktopShare/issues/294). This is the part of work-stream H that was deferred, not cut: `nvidia-dgx-spark-local-kb.md` §5 named which workloads move off Anthropic tokens and how to measure it, measured one pair, and parked the rest. L turns that into a standing program. **L1 is built:** `files/issue-226/kb/offload.py` reads everything Claude Code has run on `spark-dd06` against the box's own vLLM counters and appends one row to `files/issue-226/kb/offload-ledger.jsonl`. **First reading, 2026-09-02 19:56 UTC:** the box's model did **1.50 %** of generation (35,295 tokens against Claude's 2,310,884 across 101 sessions), and a session had invoked the KB **twice**. Building the scoreboard threw out the hand-count the issue was filed with, which is why it went first (§2). **L2 is built (same day):** `.claude/hooks/kb-retrieve.sh` runs every prose grep a session makes — a Bash `grep`/`rg` here; the `Grep` tool has never been called on this box — through the box's index at the call site and injects the top cited sections — the one mechanism sessions demonstrably follow — with every retrieval logged so the scoreboard can tell whether the passages displace reads or just add to them (§3). L3–L4 are planned, not built.

## 1. The number that has to move, and the one that does not

Steven asked how much of our sessions had run on the box's model instead of Claude. The honest answer is *almost none*, and the first version of the answer got the size of "almost none" wrong twice, so the numbers below come from the scoreboard, not from a grep.

**What I saw.** Every Claude Code session on this box since it landed — 101 of them, 2026-08-26 to 2026-09-02 — recorded in `~/.claude/projects/**/*.jsonl`, against the cumulative counters the box's vLLM publishes on `:8000/metrics`:

```text
# as-built 2026-09-02 19:56 UTC — offload.py snapshot, row 1 of the ledger
Claude Code   101 sessions 2026-08-26→2026-09-02, 2,935 assistant messages (deduped by message.id)
              output 2,310,884   input: fresh 51,288 / cache-read 489,147,905 / cache-create 13,487,270
Box model     nvidia/Qwen3.6-35B-A3B-NVFP4  (counter since 2026-08-28 14:36 UTC; every caller = CEILING)
              generation 35,295   prompt 413,244   requests 192
kb_search     2 calls in 2 sessions (2.0 %)
GENERATION OFFLOAD RATIO  35,295 / (35,295 + 2,310,884) = 1.504 %
```

**Why the 503 million input tokens are not the target.** 97 % of Claude's input is cache-read: the agent loop re-reading its own context on every turn, billed at 0.1× ([Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing)). That is the cost of Claude *being the orchestrator* — holding the tool loop, the judgment, the cross-device state — and `nvidia-dgx-spark-local-kb.md` §5's own verdict keeps that hosted: doc authoring and cross-doc synthesis stay on Claude, and the loop that drives them goes with it. Moving the orchestrator to a 35B model is not the plan.

**What the target is.** Generation share: the tokens the box's model *produced* against the tokens Claude produced. That is the work that can move — §5's "Move" rows are extraction, first-pass lint, "which doc solves this" retrieval and log triage, all mechanical, all high-volume, all checkable. Today that share is **1.50 %**. Its ceiling is well under half, because authoring stays hosted; the §5.1 pair already showed why — the box was ~500× cheaper and ~10× faster on a lint pass and lost on quality with a false positive. The program is to move the mechanical half and prove, row by row, that it moved.

**The finding that changes the order.** The KB — §5's flagship "Move" row, built in H, wired into `.mcp.json` and named as a rung in `CLAUDE.md`'s pattern ladder — has been invoked by a session **twice** in 101 sessions. Retrieval is not losing to reading whole files; it is not being tried. That makes L2 the sharpest rung, not the cheapest.

## 2. The scoreboard — what a row is, and what it is honest about

`files/issue-226/kb/offload.py` sits next to `measure.py` and reads the same endpoint the same way. `measure.py` prices one workload; `offload.py` scores the whole box.

```bash
# on spark-dd06, from the repo root
python3 files/issue-226/kb/offload.py snapshot            # take a reading and append it to the ledger
python3 files/issue-226/kb/offload.py snapshot --dry-run  # print the reading without recording it
python3 files/issue-226/kb/offload.py table               # the ledger as a markdown table with per-window deltas
```

A row holds three sources and two ratios. Nothing in it leaves the box.

| Source | What is read | The trap it avoids |
|---|---|---|
| vLLM `:8000/metrics` | `generation_tokens_total`, `prompt_tokens_total`, `request_success_total`, `process_start_time_seconds` | The counter counts **every caller** — the validator, `measure.py`, any app — and resets when the server restarts. So it is recorded as a **ceiling** on the box's generation, and the process start time travels with it: `table` shows a restart as a break (a floor on the window's box tokens), never as a drop |
| `~/.claude/projects/**/*.jsonl` | `usage` on each assistant message | Claude Code writes one turn once per content block — a text line, then a tool-use line — with the **same `usage` on each**. A raw sum overcounts; on this box the factor was **3.03×** (6.98 M raw against 2.30 M real). Rows dedupe by `message.id` |
| the same transcripts | `tool_use` blocks named `mcp__ds-kb__kb_search` | The tool's *name* appears 120+ times — in hook injections, in tool results, in prose about it. Only two of those were invocations. Rows count blocks, not strings |
| `~/.claude/kb-retrievals.log` | one JSON line per retrieval the L2 hook made at a `Grep` call site (`kb-retrieve.sh` → `kb_hook.py`) | A session that never *calls* the tool can still have been *served* by it. KB adoption counts a session once if it had either, and `table` prints `(tool·hook)` so the two are never confused. Synthetic test sessions are excluded |

The two ratios: **generation** = box generation ÷ (box generation + Claude output), and **KB adoption** = sessions with at least one real `kb_search` ÷ all sessions. Both are cumulative in the row; `table` prints the delta between consecutive rows, and that windowed ratio is the one each rung is gated on — the cumulative number carries the whole history and moves slowly by construction.

Two ledgers, both committed: `offload-ledger.jsonl` is the snapshot series of the whole box (`table`); `offload-workloads.jsonl` is one row per real run of a rung's tool — `compress.py` for L3 — with input size, chunks, the box's tokens, latency, and the hosted input avoided (`workloads`). Both are the durable record; the issue thread is where the reasoning about a row lives.

**The ratio measures quantity, not value — and L4 proved it.** Row 5's window read 40.85 % because the box produced 18,236 tokens of lint findings and extracted claims that adjudication then mostly rejected. A rung counts only when its quality anchor passes (`doc-check.py`, or Claude's adjudication recorded in the issue); the ratio on its own can be gamed by volume, and the box will happily supply the volume. Read the two together or read neither. The "hosted input avoided" column uses the box's tokenizer as the proxy for Anthropic's: chars/4 undercounts logs by ~2× (42,029 chars measured 19,887 tokens, not 10,507), so the measured number is the one recorded.

**Two hand-counts this replaced.** The issue was filed with 0.52 % and "68 calls in 29 sessions". Both came from grepping: summing every `usage` line, and matching the tool name anywhere in a transcript. The first row of the scoreboard disagreed with both, and the scoreboard was right — the *actual* ratio is 1.50 % (Claude did a third of what the grep said) and the KB has been used twice. That correction is the argument for building the measurement before building any lever.

## 3. The rungs — §5's rows, executed in order

Each rung is a row in `nvidia-dgx-spark-local-kb.md` §5's table with its verdict already written; L builds what the verdict said and gates it on the scoreboard. Same one-rung-at-a-time discipline as every other work-stream here.

| Rung | §5 row and verdict | What lands | Gate before the next rung |
|---|---|---|---|
| **L1 ✅** | the measurement plan | **Built 2026-09-02.** `offload.py`, the ledger, row 1. The counter-as-ceiling and dedupe-by-message-id decisions above | **Met on the numbers, open on the pair:** row 1 exists and reproduces on a re-run; a second dated row lands at the end of the same session so the delta mechanism is shown live. The *full authoring-chain* pair — one document through all of `files/issue-226/authoring-workflow.js` with per-phase tokens on both chains (§5 steps 1–3) — is scheduled as the ledger's first *workload* row, not done here |
| **L2 ✅ built / ⏳ gate** | "which doc solves this" → the KB (**Move**, flagship) | **Built 2026-09-02 — retrieval at the call site.** Row 1 said the tool was built, wired, on the ladder, pushed at sessions by `guard.sh` rule 11 — and invoked twice; the two "uses" were the H3 self-test and a mis-aimed query. Instructions were tried and measured, and the session that built the scoreboard got the injection and grepped anyway. So L2 does not ask; it does. `.claude/hooks/kb-retrieve.sh` (PreToolUse on `Bash` and `Grep`) runs the pattern a session was about to grep for through `files/issue-226/kb/kb_hook.py` (TEI + Qdrant directly, no MCP, ~20 ms) and injects the top-3 cited sections, so the session goes to the section instead of chaining grep → Read × 5. The call site had to be measured too: on this box the `Grep` tool has been called **zero** times in 102 sessions — the move is a Bash `grep`/`rg`, 906 of 2,400 Bash calls — so the hook parses the Bash command (a grep that searches repo files, never one filtering a pipe) and a hook on the tool alone would never have fired. It is its own hook, not a `guard.sh` rule: only one context can be emitted per call there and rule 11 owns it; as a separate hook both land. spark-dd06-only, fail-open, hard timeout, once per (session, query), every retrieval logged, and **nothing injected unless the top hit scores ≥ 0.55** — the first live fire returned three ~0.50 passages for a topic the index did not yet hold, and weak passages are the noise the gate exists to catch. Deferral friction removed box-locally (`ENABLE_TOOL_SEARCH=auto` in settings.local.json; per-tool un-deferral does not exist in the CLI). Corpus widening waits until adoption produces signal | Windowed KB adoption ≥ 50 % of sessions (from 2 %), **and** Claude cache-create tokens per session trending down across at least three rows — injected passages are extra input, and they have to *displace* reads, not add to them |
| **L3 ✅ built / gate met** | log triage → summarize on the box (**Move**) | **Built 2026-09-03 — context-compression pre-pass.** `files/issue-226/kb/compress.py` reads a dump (stdin or `--file`), chunks it at 60 K chars, triages each chunk on the box's model with reasoning off, merges, and returns VERDICT / ERRORS (distinct signatures, count, one raw line each) / WARNINGS / TIMELINE / NEXT — raw lines pasted, never paraphrased, because every incident rule here runs on the exact text. Two real dumps: the healthy `mynifi-0` stdout, and the crash-looping broker `my-cluster-combined-0`'s 1.28 MB `--previous` log — **429,745 tokens in, 1,009 out, 170 s, 22 chunks**, the fencing/election timeline and the duplicate-registration exception found with raw lines, and the right next commands (check brokers 1 and 2 — which are the ones OOM-killing). `agent/workflow.md` already said to send a cheap agent so a dump never enters main context; this makes it free. **Follow-on, not done:** the call-site offer — a hook that suggests `compress.py` when a Bash `kubectl logs`/`journalctl` has no `head`/`tail`/`grep` (a settings entry, Steven's approval) — and prepending the pod's `lastState`/`restartCount` so an external `OOMKilled` is visible to the triage | **Met:** two real dumps end-to-end on the box with tokens in `offload-workloads.jsonl`; hosted input avoided ≈ 448 K tokens; the vLLM counter moved +10,462 generation for the window (row 4), the first window in which the box's share registered — **29.8 % windowed**, against 0.00 % and 0.19 % for the two windows before it. That window is the session that ran L3, so it shows the scoreboard scores a lever, not a trend |
| **L4 ✅ measured / ✗ gate not met** | extract (**Move**, highest volume) + adversarial fact-check (**Hybrid**) | **Measured 2026-09-03 — local generation, hosted adjudication, on this doc.** `measure.py lint`: 13 findings in 6.1 s, 416 tokens out — **0 actionable.** Ten apply the blog rulebook to a plan doc (front matter, Jekyll closer, `{: .notice}` classes, a "How to…" title), two are matters of taste, one is a hallucination (every fence is tagged; `doc-check` proves it). Systematic, not random: it was handed `agent/writing-style.md`, which is blog-centric, with no plan-doc convention. **Rulebook built (same day):** `plan-doc-rules.md` v1 removed every category error and every hallucinated quote (7 findings on this doc, 2 real) but over-fired Rule 4 on every definite noun in the efm-agent doc ("the Jetson" → a wrong name, 25 times) and reasoned out loud into a 2,500-token truncation; v2 narrowed Rule 4 to *missing* values, declared fleet names specific, banned non-findings and think-aloud, and capped at 10. `measure.py extract`: unbounded, it atomized the doc into 300+ claims and truncated at both a 2,500 and an 8,000-token cap; bounded to "at most 60, no restatement", it returned **249 claims with 48 near-duplicate pairs** — the budget ignored — but closed its JSON, and a seeded 15-claim sample adjudicated against the doc was **13 correct, 1 semantically degraded, 1 duplicate**. Three harness fixes fell out: the doc-check anchor's hardcoded `2026-08-27` status date (a false "1 error" on every doc since) now reads the doc's own; a per-workload output cap; rows to `offload-workloads.jsonl`. **Dedupe built (same day), in `measure.py`:** word-set overlap > 0.7 *and* no number, identifier or path in the difference; the longer claim survives; complete objects are salvaged from a truncated array. Measured on the saved 249-claim run: **249 → 231, and 12 of the 17 drops were true restatements, 5 were distinct short facts sharing a frame** ("a piped grep is silent" into "a grep aimed outside the repo is silent"). The first rule — overlap alone — removed 40 but merged "provides `prompt_tokens_total`" into "provides `generation_tokens_total`": about 3 in 8 drops lost a fact. A fresh run: 313 raw → 289; its sample 12 of 15 correct, one a mangled URL (`about-clude`). So the dedupe is safe and small: 7–10 % removed against a volume 4–5× the budget. **Per-section extraction (same day) is the fix.** `measure.py extract-sections` splits at `## `, gives each section a budget sized to its length (words ÷ 60, clamped 3–15), adds a self-containment rule, and tags every claim with its section. Same doc, same box: **10 of 10 sections within budget, 0 truncated; 70 raw → 69 deduped** (one near-duplicate — the dedupe is barely needed); **22 s** against 63 s; **2,481 output tokens** against 8,000; claims of six words or fewer — the shard proxy — **12 % against 43 %**; a seeded 15-claim sample **15 of 15 correct**, every one self-contained. The model honours a budget it can see the edge of. **Hosted pair** (a Sonnet agent, same doc, same per-section rules): **70 claims, 10 of 10 within budget, no shards**, denser compound claims and verbatim URLs — in 94 s and ≈ 67 K agent tokens, against the box's 22 s and 11.7 K tokens at no cost. Correctness at parity; density and selection still hosted's. Full pair in the issue thread | **Lint: rulebook built and measured, gate not met.** `files/issue-226/kb/plan-doc-rules.md` replaces the blog style guide in the prompt — what a plan doc *is*, what `doc-check.py` already checks, the eight voice rules that still apply, verbatim quotes only. Two iterations, two docs. On `nvidia-dgx-spark-efm-agent.md`, where the §5.1 hosted pass found four applicable violations, v2 returned 8 findings: **5 real, and 3 of the 4 hosted findings recovered** (weak voice, opening frames *what*, *why* not in the block; it missed the generic headers and flagged a good one instead), 9 s, not truncated. On this doc, `doc-check` clean, it returned **exactly one finding per rule — 2 real of 8**: it fills the format and cannot say `clean`. Zero hallucinated quotes in either. A cheap pre-screen on a doc with real problems; adjudication mandatory; one hosted finding missed, so not moved. **Extract: quality anchor passed on the per-section run** (15 of 15, within budget, shards 12 %) after failing whole-doc (80–93 %, 4–5× the budget). It moves as the first pass, with Claude curating ≈ 70 section-tagged claims per doc — §5's "Split" and "Hybrid" verdicts, executed. Nothing is claimed on the windowed ratio |

L1 first because a lever with no scoreboard is faith. L2 next because row 1 says it is the biggest gap. L3 is the only rung that also dents the cache-read load. L4 carries the quality risk the §5.1 pair measured, so it goes last and cannot pass on cost alone.

## 4. What this does not touch

- **The orchestrator.** Sessions stay on Claude. This measures and moves the mechanical work a session hands out, not the session.
- **Doc authoring.** Stays hosted, per §5. Whether a *bigger* local model could take it on is the model-evaluation question under work-stream A ([#232](https://github.com/cldr-steven-matison/DesktopShare/issues/232)).
- **The GPU-services cutover.** Work-stream F's §9 ladder is a planning deliverable and remains one (`nvidia-dgx-spark-plan.md` §6, 2026-08-27).
- **Anything outside this track's own KB.** `ds-kb` / `desktopshare-kb` is the DGX track's retrieval over this repo's docs and agent rules. The Streamers demo is a separate track with its own knowledge base; nothing here reads it, writes it, or cites it.
- **RAG itself.** Retrieval exists (`nvidia-dgx-spark-local-kb.md` §3). L2 changes how often it is *called*, not how it works.

## 5. What NOT to do

- **Do not sum `usage` across transcript lines.** One assistant turn, several lines, identical `usage` on each. Dedupe by `message.id` or the ratio is wrong by 3×.
- **Do not count a tool by grepping its name.** Hook injections and tool results carry the name too. Count `tool_use` blocks.
- **Do not read the vLLM counter as "what sessions used".** It is every caller on the box since the last restart. It is a ceiling, and a restart is a break in the series — which is why each row carries `process_start_time_seconds`.
- **Do not gate a rung on the cumulative ratio.** It carries the whole history and barely moves. Gate on the windowed ratio `table` prints.
- **Do not let a rung pass on cost.** The §5.1 pair was ~500× cheaper locally and worse. `doc-check.py` is the quality anchor for L4; L3's gate is a real dump triaged correctly, not a cheap one.
- **Do not hook the `Grep` tool and call it done.** Sessions on this box have never called it — 0 of 2,400+ tool calls; they grep through Bash. Measure the call site before wiring the hook, the same way the scoreboard measured the ratio before the levers.
- **Do not inject on `Read`.** A PreToolUse hook cannot substitute a result, only add context — passages injected on a Read are tokens on top of a read that still happens. Inject on `Grep`, the "where is X" move, where the cited section displaces the reads that would have followed. Whether it does is the L2 gate, not an assumption.
- **Do not read a log into hosted context to learn whether it is healthy.** The broker log was 429,745 tokens on the box's tokenizer — more than a context window — and its triage crossed over as 1,009. Compress first; then read the raw lines the triage cites.
- **Do not let log-only triage rule on a crash.** The broker's own log ended mid-INFO at 13:10:01; `OOMKilled exit 137` at 13:10:13 lives in the pod status. Pair the summary with `kubectl get pod … -o jsonpath='{.status.containerStatuses[0].lastState}'` — free and deterministic. And on this box `kubectl` needs `KUBECONFIG` pointed at the k3s config under /etc/rancher/k3s/ (world-readable), or it silently talks to TEI on `:8080`.
- **Do not run the `NEXT` command blindly.** Both runs proposed plausible commands; the control's was wrong (the NiFi API needs the client cert, not `curl -k`). It is a suggestion from the mechanical tier, exactly the §5.1 verdict.
- **Do not lint a plan doc against the blog rulebook and act on it.** The local pass returned 13 findings and 0 were real; it does not know a root-tier plan doc from a `_posts/` article unless told. Give it the plan-doc convention or restrict it to what `doc-check.py` can verify.
- **Do not dedupe extracted claims by word overlap alone.** Overlap > 0.7 merged "provides `prompt_tokens_total`" into "provides `generation_tokens_total`" — a fact gone. Guard on content tokens in the difference (numbers, identifiers, paths), bias to keeping, and still expect about 1 in 4 drops to be a distinct short fact. A surviving redundant claim costs a few tokens; a merged fact is lost.
- **Do not trust a local lint that returns one finding per rule.** On a `doc-check`-clean doc the rulebook pass returned exactly eight findings for eight rules, two real: it fills the format and cannot say `clean`. Adjudicate every finding, and read a one-per-rule shape as a sign the doc is probably fine.
- **Do not extract a whole document in one call.** Whole-doc, the model ignored its budget four times running (249–325 claims for "at most 60") and 43 % of what it produced were context-free shards. Per section — a budget it can see the edge of — it stayed within budget 10 of 10 times and shards fell to 12 %. The unit of extraction is the section.
- **Do not accept an unbounded extract.** Asked for at most 60 distinct claims, the model returned 249 with 48 near-duplicates; unbounded it did not finish at 8,000 tokens. The facts were right; the volume was not. Dedupe deterministically, then curate.
- **Do not reprint a savings headline.** §5 step 5 still stands: the only dollar figure in the corpus is a relayed X post. The ledger's own rows are the only numbers this doc will ever quote.

## Open questions

- **Attribute the counter by caller, or keep the ceiling?** vLLM does not tag requests. Tagging would mean every local caller passes a marker (a `user` field, or a header the validator and `offload.py` agree on) and a small log at the endpoint. The ceiling is honest and cheap; attribution is exact and adds a moving part. L1 chose the ceiling and records the choice; revisit when a rung's windowed ratio is close enough to its gate that the validator's share matters.
- **What is L2's realistic ceiling?** Retrieval replaces *reads*, and a read is a cache-create event, not an output token. The generation ratio may barely move on L2 while cache-create per session drops — which is why L2's gate is adoption plus cache-create, not the generation ratio.
- **How to bound the model's output when it ignores the budget.** "At most 60 claims" produced 249. Options: a hard `max_tokens` sized to the budget and accept a closed-but-partial array; per-section extraction with a small per-section cap; or a deterministic dedupe after the fact (the 70 %-shared-words check found the 48 pairs in milliseconds). The dedupe is the cheapest and does not depend on instruction-following. *Measured 2026-09-03:* built — it removes 7–10 % and cannot reliably tell a restatement from a distinct short fact even with the content-token guard (1 in 4 drops). The volume is atomization, not repetition. *Closed the same day:* per-section extraction with a per-section budget — 10 of 10 within budget, 70 claims, 15 of 15 sampled correct. The unit the model can be bounded on is the section.
- **How to let the local lint say `clean`.** Told to output `clean` when nothing violates rules 1–8, the model produced one finding per rule instead. A two-stage pass — list candidate quotes, then judge each — or leaving the clean verdict to the hosted adjudicator are the options; untested.
- **Does compression pay on the box's context window?** The lead model runs `max_model_len` 262,144 (`:8000/v1/models`). Summarizing a `flow.json.gz` locally is cheap; whether the summary is *sufficient* for the edit that follows is the L3 measurement.

## Definition of done

- ✅ `files/issue-226/kb/offload.py` exists next to `measure.py`, reads the three sources above, dedupes by `message.id`, counts `tool_use` blocks, records `process_start_time_seconds`, and appends to `files/issue-226/kb/offload-ledger.jsonl`. `snapshot`, `snapshot --dry-run` and `table` all run on `spark-dd06`.
- ✅ Row 1 is in the ledger: 1.504 % generation, 2/101 KB sessions, 2026-09-02 19:56 UTC.
- ✅ Row 2, same session (20:00 UTC): `table` shows the live delta — box +0, Claude +16,092, **windowed ratio 0.00 %**. The session that built the scoreboard ran entirely on Claude, which is the finding restated as a row.
- ✅ The issue, the spine's §4 row and decision log, and the box's memory carry the scoreboard's numbers, not the grep's.
- ✅ `python3 files/issue-226/doc-check.py --repo . --research-dir files/issue-226/research --status-date 2026-09-02 nvidia-dgx-spark-offload.md` reports zero errors.
- ✅ L2 built: `kb-retrieve.sh` registered as a PreToolUse hook on `Bash|Grep` in `.claude/settings.json`; synthetic payloads behave — a Bash grep of repo prose injects three cited sections and logs in ~20 ms; a piped grep (`docker ps | grep`), a grep aimed outside the repo, a repeat of the same query, a code glob and a non-matching tool are all silent; `offload.py` scores hook retrievals as `(tool·hook)` and excludes test sessions.
- ✅ L2 fires live: registered mid-session, a real `grep -nE … nvidia-dgx-spark-offload.md` in the same session came back with the `[local KB · retrieve, don't read]` context injected — no restart, no `/hooks`. That fire is also what produced the 0.55 score floor.
- ⏳ L2 gate: windowed adoption ≥ 50 % and cache-create per session trending down over at least three rows — measured over the coming sessions, not claimable today.
- ✅ L3 built and gate met (2026-09-03): `compress.py` triaged two real dumps on the box — `mynifi-0` stdout 19,887 → 275 tokens in 3.4 s with the correct healthy verdict, and the crashed broker's 1.28 MB log 429,745 → 1,009 tokens in 170 s with the real timeline and exceptions pasted raw — both rows in `offload-workloads.jsonl`; hosted input avoided ≈ 448 K tokens.
- ⏳ L3 follow-on: the `kubectl logs` call-site offer (a hook; needs a settings entry) and the pod-status prepend so an OOMKill is visible to the triage.
- ✅ L4 measured (2026-09-03): `measure.py lint` and `extract` run on this doc, four rows in `offload-workloads.jsonl`, every finding adjudicated by Claude against the doc and `agent/writing-style.md`; the three harness fixes (doc-check status date, output cap, ledger rows) landed.
- ✗ L4 gate not met, and recorded as such: lint 0/13 actionable under the blog rules; under `plan-doc-rules.md` v2, 5/8 real with 3 of 4 hosted findings recovered on a doc with problems and 2/8 on a clean doc (one finding per rule) → a cheap pre-screen, adjudication mandatory, one hosted finding missed, not moved; extract 80–93 % facts correct whole-doc but 4–5× the claim budget → fixed per-section (below). Nothing is claimed on the windowed ratio.
- ✅ L4 lint rulebook built and measured (2026-09-03): `files/issue-226/kb/plan-doc-rules.md`, two iterations on two docs, four rows in `offload-workloads.jsonl`; zero hallucinated quotes under it.
- ✅ L4 extract dedupe built and measured (2026-09-03): deterministic, in `measure.py`; on the saved 249-claim run 249 → 231 with 12 of 17 drops true restatements under the tightened content-token rule (the overlap-only rule removed 40 with ~3 in 8 false merges). Safe, small — 7–10 %.
- ✅ L4 per-section extraction built and measured (2026-09-03): `measure.py extract-sections` — 10 of 10 sections within budget, 0 truncated, 70 raw → 69 deduped, 22 s, 2,481 output tokens, shards 12 %, seeded sample 15 of 15 correct. Extract's quality anchor passes; it moves as the first pass with Claude curating. The hosted pair on the same rules is in the issue thread.

## When this ships

- [#294](https://github.com/cldr-steven-matison/DesktopShare/issues/294) stays open through L4; each rung closes with a ledger row and an issue comment. The work-stream L row in `nvidia-dgx-spark-plan.md` §4 flips per rung.
- `nvidia-dgx-spark-local-kb.md` §5 gains a one-line pointer to this doc as where the standing measurement lives; §5's table and its verdicts stay where they are.
- When L2 lands, `CLAUDE.md`'s "Finding the pattern you need" ladder and `agent/workflow.md` say *how* the KB rung is meant to be used, not only that it exists — row 1 says the rung is there and nobody climbs it.
- ch17 of `files/nvidia-spark-guide/README.md` takes the ledger's rows as its measured numbers, replacing the §5.1 seed pair as the chapter's evidence.

## Resources

- `nvidia-dgx-spark-local-kb.md` §5 — the workload table and verdicts this executes; §5.1 the seed pair; §4 the validator L4 builds on.
- `files/issue-226/kb/measure.py` — per-workload local-vs-hosted pricing, the sibling of `offload.py`.
- [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing) and [Claude Code costs](https://code.claude.com/docs/en/costs) — the cache-read multiplier and the PreToolUse-filter cost lever L3 takes.
- `nvidia-dgx-spark-plan.md` §4 and §6 — the work-stream table and the decision that keeps the orchestrator hosted.
