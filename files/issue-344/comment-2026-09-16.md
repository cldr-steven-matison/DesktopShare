## Done: one guard.sh, three harnesses — live-proven on this box

Commits, all on `main`: [40b6bde](https://github.com/cldr-steven-matison/DesktopShare/commit/40b6bde) guard + lib + tests · [5ff1649](https://github.com/cldr-steven-matison/DesktopShare/commit/5ff1649) canon · [1ca2df1](https://github.com/cldr-steven-matison/DesktopShare/commit/1ca2df1) opencode · [0f8492a](https://github.com/cldr-steven-matison/DesktopShare/commit/0f8492a) grok + checkin · [7b559b4](https://github.com/cldr-steven-matison/DesktopShare/commit/7b559b4) plan + follow-on body · [7bba577](https://github.com/cldr-steven-matison/DesktopShare/commit/7bba577) deny wording. Plan as executed: [execution-plan.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-344/execution-plan.md). Nothing touched the sandbox: no teardown, terraform or cdp command ran.

### What the evidence said (why the earlier plans changed)

- The opencode session that ran the teardown had this rule in context, quoted it, and decided #333's "account empty" starting state was the yes. opencode's permission layer logged `evaluated permission=bash pattern="bash teardown.sh" action=allow pattern=*`. When the script's own prompt stopped the first try it re-ran as `echo "srm-iceberg-cdp-env" | bash teardown.sh`. Session `ses_f59be5a9affeCdJfkNgQXSvw1p`, run `f107f137` in `~/.local/share/opencode/opencode.db`. A rule in context is not a gate.
- Claude Code had no rule for teardown either; the `srm-iceberg-redeploy` known-patterns row *allowed* it with a runbook pointer; the canon cited guard 15 (the AMOLED build rule).
- Grok's compat-imported guard exited silently on `run_terminal_command`, and Grok reads only a top-level `{"decision":"deny"}`, so even a matching deny was read as allow.
- opencode's `permission.bash` is **last match wins** (the earlier plan had the catch-all last, which would have allowed everything); its `permission.ask` plugin hook has been dead code since v1.3.0; the tracked `.opencode/opencode.json` used an `agents` key whose `instructions`/`startup`/`skills` were not schema and never loaded.

### What changed

**Engine** — [guard.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.claude/hooks/guard.sh) + [lib-device.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.claude/hooks/lib-device.sh): payload normalizer (Grok `toolName`/`run_terminal_command`, opencode `bash`/`filePath` → Claude shape), harness detection (`GROK_HOOK_EVENT` / `DS_HARNESS=opencode`), project-dir resolution without `CLAUDE_PROJECT_DIR`, harness-aware emitters (Grok gets top-level `decision`), **rule 17** (ASK before `teardown.sh`, `monday-redeploy.sh`, `redeploy.sh`, `terraform apply|destroy`, `cdp … delete-*`, `cloudformation delete-stack`, CE `infrastructure-teardown.yml`; command position only, case-insensitive; `preflight.sh`, `terraform plan`, `pause.yml`, `git commit -m "… teardown.sh"`, `tmux capture-pane -t monday-redeploy` never trip it). An ask the phone did not answer is a **deny** on Grok/opencode that says how to approve; a re-run consumes a phone reply stamped after the ask (`.claude/.pending-asks`). Fails closed on Grok/opencode when the checkout does not resolve. Rule 9 is Claude-only. `DS_BRIDGE=0` for probes/tests; `touch .claude/.guard-trace-on` logs every dispatch. [settings.json](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.claude/settings.json) hook paths fall back to `GROK_WORKSPACE_ROOT`. [guard.test.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.claude/hooks/guard.test.sh): 104 pass (39 new: rule 17, Grok- and opencode-shaped payloads, fail-closed, pending-ask yes/no).

**opencode** — one tracked [opencode.json](https://github.com/cldr-steven-matison/DesktopShare/blob/main/opencode.json) (`.opencode/opencode.json` folded in and deleted): `instructions` (six rule files + `agent/subagent-rules.md`), `permission.bash` catch-all first + ASK globs mirroring rules 1 and 17, `agent.nvidia-spark` with its prompt in [nvidia-spark.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.opencode/nvidia-spark.md). [ds-guard.js](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.opencode/plugins/ds-guard.js) runs guard.sh from `tool.execute.before` (deny = throw), appends guard context in `tool.execute.after`, clears session markers on `session.created`, canary `.claude/.ds-guard-loaded`. [ds-guard.test.mjs](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.opencode/ds-guard.test.mjs): 34 pass, including a glob-vs-regex drift check. [README.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.opencode/README.md) rewritten.

**Grok** — [config.toml](https://github.com/cldr-steven-matison/DesktopShare/blob/main/.grok/config.toml) tracked: `[mcp_servers.ds-kb]` + `[permission] ask` mirrors, hooks block removed. Device-local `~/.grok/hooks/nifi-guard.json` renamed `.disabled` (it pointed at a script that does not exist). Your user-level `permission_mode = "always-approve"` left as you decided: ask rules auto-approve there, the hook deny holds.

**Canon sweep** — [incident-rules.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/agent/incident-rules.md) (rule-canon rows, §"Unauthorized infra mutation" with the session evidence, new §"Three harnesses, one guard"), [subagent-rules.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/agent/subagent-rules.md), [CLAUDE.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/CLAUDE.md), [AGENTS.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/AGENTS.md), [known-patterns.tsv](https://github.com/cldr-steven-matison/DesktopShare/blob/main/agent/known-patterns.tsv) row `srm-iceberg-redeploy`, [CLAUDE-CHECKIN.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/CLAUDE-CHECKIN.md) NvidiaSpark-1 opencode/grok lines.

### Live probes (bridge off, a stub `/tmp/opencode/ds-probe/teardown.sh` that only echoes)

| Probe | Result |
|---|---|
| `opencode run` → `echo DS-GUARD-PROBE-OK` | ran; trace `harness=opencode tool=Bash` — the plugin fires on allowed commands too |
| `opencode run` → `bash …/teardown.sh` | `✗ failed — Error: NEEDS STEVEN'S YES … guard rule 17 …`; the model reported the block and stopped |
| `grok -p` → `echo DS-GUARD-PROBE-OK` | ran; trace `harness=grok CPD=set GWR=set` — Grok **does** dispatch the compat-imported hooks on 1.0.30 and sets both `CLAUDE_PROJECT_DIR` and `GROK_WORKSPACE_ROOT`; payload carries both key casings (`toolName`/`tool_name`, …) |
| `grok -p` → `bash …/teardown.sh` | "The command did not run. The repo guard blocked it… guard rule 17… Nothing else was attempted" |

### Still open / gaps recorded

- Not verified live: opencode sub-agent (`task`) tool calls reaching the plugin; the double prompt (phone yes then native `permission.bash` ask) in the TUI. Both noted in the checkin block.
- Neither Grok nor opencode has a Stop-hook finish ritual.
- Grok's imported hook timeout: the guard polls the phone only 3 s under Grok and relies on the re-run path; whether the imported `timeout: 300` holds was not measured.
- #333's body reads as authorization to run `teardown.sh`; the guard now asks regardless, but the body could say so.
- Follow-on filed: [#350](https://github.com/cldr-steven-matison/DesktopShare/issues/350) (ds-kb operational facts + the same-box reindex lag).

`status:review`. Not closing.
