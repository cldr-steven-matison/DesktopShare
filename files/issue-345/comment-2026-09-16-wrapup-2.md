**Delivered from the DGX Spark: Ozone root-caused and fixed live, sizing evaluated, cluster kept running. Commit [dee8acd](https://github.com/cldr-steven-matison/DesktopShare/commit/dee8acd).**

**Ozone, closed.** The source analysis above held and the live test confirmed it. With `hdds.grpc.tls.enabled=false` in the Ozone service safety valve and Ozone restarted (15:30 UTC), the SCM log shows zero `Setting TLS`, zero `No name matching`, zero `Invalid domain`; `scm roles` lists master-01 LEADER with master-02 and master-03 FOLLOWER, `om roles` the same three, four DataNodes registered, safemode off, and a key written to `o3://o3service1/ch18/smoke/hello.txt` at replication THREE and read back. Evidence: [ozone-after-tls-off-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/ozone-after-tls-off-2026-09-16.txt), [restart-final-valve-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/restart-final-valve-2026-09-16.txt). The follower cert fetch (`RAFT is closed`) was a consequence of the dead ring, not a second defect ([restart-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/restart-2026-09-16.txt) has the stack). `name_prefix` is not a factor and no redeploy was spent on it.

Two things the first attempt taught: `hdds.grpc.tls.enabled` is not a CM parameter you can PUT (it is generated from the roles' `ssl_enabled`, which AutoTLS sets), and CM writes the valve *before* the generated value, so the valve needs `<final>true</final>` or the generated `true` wins. The first restart without `final` changed nothing. Now template edit 12 in [ozone-nifi-cluster.yml](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/ozone-nifi-cluster.yml) ([diff](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/ozone-nifi-cluster.diff)) and runbook §3.1/§4.1 ([cloudera-ce-aws-runbook.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/cloudera-ce-aws-runbook.md)). The alternative that keeps ring TLS, an IANA-TLD `dns_domain` plus the Knox whitelist edit, needs a redeploy and is documented, not run.

**One follow-up for you:** run **Deploy Client Configuration** on the cluster in CM. The deployed `/etc/ozone/conf` on sdx-01 still carries the generated TLS `true` from 11:35, so `ozone sh` from the gateway host hangs against the now-plain DataNodes; the write proof above used the OM's process config on master-01.

**Sizing (your ask).** Stock `t3a.xlarge` workers (16 GB, no swap) carry 19 GB of CM default heap once NiFi is grafted in (DN 4 + NiFi 4 at `-Xms`=`-Xmx` + RS 4 + Ozone DN 4 + Kafka 2 + NM 1, plus 8 GB advertised to YARN). Three of four workers wedged between 12:24 and 13:38 UTC (EC2 `impaired`, no CM heartbeat, SSH banner timeout) and worker-02 OOM-killed its DataNode at 14:20. CPU credits were not the constraint. Next run: workers `r5a.2xlarge` (8/64 GB), masters and sdx `r5a.xlarge` (4/32 GB), edited in `tf_cluster_aws/hosts_base.tf` (the config file cannot override instance types), about $3.5/h instead of $2/h. Evidence and the price table: [worker-sizing-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/worker-sizing-2026-09-16.txt); runbook §2a.

**What kept the cluster alive on 16 GB for the validation, on your call:** Atlas, Hive, Hive-on-Tez, YARN and HBase stopped in CM ([stop-unneeded-services-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/stop-unneeded-services-2026-09-16.txt)); NiFi heap to 2 GB and Ozone DataNode heap to 2 GB ([config-changes-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/config-changes-2026-09-16.txt)); the three wedged workers rebooted from EC2. Current CM state: Ozone, Kafka, Ranger, Knox, Solr, ZooKeeper GOOD; NiFi 4/4 connected; HDFS reads BAD on the HA canary with one corrupt-replica block from the crashes, out of safemode.

**Also:** the ingress prefix list holds only the deploy-time corp-VPN NAT `/32`, which rotated and locked the box out this morning; the check and fix are in runbook §2 and the device register, and the earlier source-analysis comment is committed as [comment-2026-09-16-source-analysis.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-345/comment-2026-09-16-source-analysis.md).

**Not done / owed.** Teardown not run (cluster kept on your call, billing since 02:32 UTC). The Deploy Client Configuration step above. The runbook's sizing is documented, not applied to `hosts_base.tf`. The 09-14 CM build was never needed in the end and stays unrecorded. Linked surfaces swept: runbook, template + diff, pattern row, device register, Ch18 chapter, source doc, tracker (the #341 comment has the chapter side).
