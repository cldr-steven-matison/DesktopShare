Item 1 ("startup from inbox to plan mode") has a one-line fix that #344 deliberately left here: add `"default_agent": "plan"` to the root [opencode.json](https://github.com/cldr-steven-matison/DesktopShare/blob/main/opencode.json) (it must be a primary agent; `plan` qualifies). Verified this session that the key exists in the installed 1.18.31 schema and resolves in `opencode debug config`.

Item 2 ("stop auto switching to Build"): opencode has no auto-switch feature (only an open feature request, anomalyco/opencode #13525); what you see is most likely new sessions starting in `build` because no `default_agent` is set, plus `opencode resume` continuing a build session. Fixing item 1 should remove it; if it persists, capture the session id and the log line from `~/.local/share/opencode/log/opencode.log` so it can be traced.

Context from #344: the tracked opencode config was rewritten today (single root `opencode.json`, `permission.bash` gate, `ds-guard` plugin) — make the `default_agent` edit there.
