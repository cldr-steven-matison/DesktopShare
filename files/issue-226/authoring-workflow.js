export const meta = {
  name: 'dgx-spark-authoring',
  description: 'Author the nine #226 plan docs from the research corpus (opus), each followed by a sonnet lint pass',
  phases: [
    { title: 'Author', detail: 'one opus author per doc, writes the file in place' },
    { title: 'Lint', detail: 'sonnet lint: sources, style, structure, cross-links; fixes in place' },
  ],
}

const R = args.researchDir
const REPO = '/home/tunas/DesktopShare'
const I = 'https://github.com/cldr-steven-matison/DesktopShare/issues/'

const COMMON = `
YOU ARE WRITING ONE DOCUMENT in Steven Matison's DesktopShare repo (${REPO}). Read these first, in this order:
1. ${REPO}/agent/writing-style.md — the voice and shape rules (first person, present tense, direct, real numbers/paths, no LLM tells, no "Introduction"/"Conclusion" headers, bullets only for true enumerations).
2. ${REPO}/nvidia-dgx-spark-plan.md — the EPIC spine you are a companion of; match its facts, issue numbers, phase names, and the DGX-Spark-vs-Apache-Spark naming rule.
3. The research JSON files named for you below (under ${R}); also ${R}/verify.json (three-lens verification votes on load-bearing claims: a claim with 2+ refutes is NOT to be asserted; one refute = state it with the caveat) and ${R}/critic.json.
4. The precedent docs named for you.

HOUSE DOC SHAPE (mandatory):
- '# Title' then a '> **Status (2026-08-24):**' blockquote: what this doc is, which work-stream letter and issue of EPIC #226 it belongs to, that the box has not arrived, what is decided vs expected.
- Numbered '## N. Section' bodies in operational order. Command blocks use a language tag; commands that cannot be run before the box arrives are labelled "expected — verify on the box" inside the block as a comment.
- Close with '## Definition of done' (checkable criteria), '## When this ships' (which docs/files get updated, what unblocks), '## Resources' (companion docs as backticked filenames first, then external links).
- Every number, version, tok/s, GB, date, or product-status claim links inline to the URL it came from (from the research JSON). No unsourced numbers. If the research has no source for something you want to say, say "unverified" or leave it out.
- Cross-reference sibling docs by exact backticked filename (nvidia-dgx-spark-research.md, -landscape.md, -runbook.md, -k3d-cso.md, -efm-agent.md, -local-kb.md, -cloudera-aws.md, -cloudera-demos.md, nvidia-dgx-spark-plan.md, 'Complete Developer Guide for Nvidia Spark with Cloudera.md', files/nvidia-spark-guide/README.md). Chapters are ch01–ch22 in files/nvidia-spark-guide/ — name the chapters this doc feeds.
- Naming: "DGX Spark" / "the Spark box" for the hardware, "Apache Spark" for the engine. Never bare "Spark" in a Cloudera sentence. The device is "NvidiaSpark-1".
- Internal issue numbers are fine in a plan doc (this is not a blog).

HARD RULES (you are a background agent; nobody can answer you):
- Write ONLY your target file, with the Write tool (overwrite). Do not edit any other file. Do not run git, gh, kubectl, curl, minikube, docker. Do not call AskUserQuestion — put open questions in a '## Open questions' section near the end and in your structured output.
- Never include the literal phrases "git commit" or "git push" anywhere.
- These are PLANS. Nothing you write is executed now. Where a plan step would touch a live system, write the rule the executor must follow: (a) never hand-build an EFM agent-deployer command and never reuse an agentIdentifier — only EFM's Deploy Agent CLI screen / POST /efm/api/agent-deployer/generateCommand; (b) kubectl port-forwards live as zellij panes in ~/.config/zellij/layouts/kube-service-ports-efm.kdl and a LAN-exposed port also needs a Windows Firewall inbound rule on WindowsDesktop; (c) never GET-then-PUT a NiFi processor with sensitive properties — use a Parameter Context; (d) new NiFi logic goes in its own new Process Group, never inline in a live one; (e) confirm before any restart of a live service; (f) WindowsDesktop's vllm :8000 also serves the OpenClaw Telegram bridge — do not plan its removal without the bridge repointed.
- Depth target is real: hit the line count asked for with substance, not padding. Tables for comparisons; command blocks for procedures; prose for reasoning.
- Return structured output: { file, lines, source_urls_count, chapters_fed, open_questions }.
`

const DOCS = [
  {
    file: 'nvidia-dgx-spark-research.md', ws: 'E', issue: 237, model: 'opus', effort: 'high', target: '400-600',
    research: ['r01-nvidia-docs','r02-playbooks-inference','r03-playbooks-agents-dev','r04-playbooks-data-cluster','r05-k8s-on-spark','r06-community','r07-github-examples','r08-x-posts','r09-cloudera-ai','r10-cloudera-aws','r11-cso-arm64','r12-local-kb-mcp', 'ALL GAP FILES (any other *.json in the dir except critic/verify)'],
    precedent: ['efm-nvidia-nano-research.md (the confidence-tag convention: [3-0] survived three refuters, [2-1] one refute — caveated, [med] single-sourced)'],
    spec: `NEW doc: the sourced research corpus for #226's ask items 2–5. Use EXACTLY these section headings so sibling docs can cite them:
## 1. NVIDIA documentation map — every User Guide section with URL and the one-line "what we need from it"; versions; known issues + workarounds table.
## 2. The official playbooks — all 46 NVIDIA/dgx-spark-playbooks, as a table: playbook · what it stands up · exact image/command highlights · chapter(s) it feeds · our verdict (adopt / adapt / skip and why). Then per-tier prose for the ones we adopt (inference, agents/RAG/dev, data/cluster).
## 3. Kubernetes on GB10 — k3s proven path (exact sequence), device-plugin floor and the UMA/nvmlDeviceGetMemoryInfo issue, k3d's CUDA node-image requirement, GPU Operator on arm64, what nobody has proven yet.
## 4. Community — NVIDIA forums, Spark Arena, reddit — the threads that matter, each with date, the concrete finding, and numbers.
## 5. GitHub — community integration examples — catalogue by category (engines, k8s, monitoring, benchmarks w/ leaderboard numbers, multi-Spark, RAG, coding agents, community) with how each plugs into our array.
## 6. X — the bookmarks and what else is out there — the three bookmarked posts quoted (author, date, text, numbers), then the discovered posts ranked by relevance, each quoted with its finding. Say plainly how many were found and how.
## 7. Cloudera AI — current state — AI Inference (NIM/Triton), on-prem status with version and date, AI Registry, Agent Studio + Nemotron, Workbench; API shape; 2026 release notes.
## 8. Cloudera on AWS — Public Cloud (environment, GPU instance types, DataFlow, Data Hub, Iceberg) and Base/Community Edition on EC2 (install path, image architecture, inbound paths for an external NiFi/Kafka).
## 9. Cloudera Streaming Operators on aarch64 — what is CONFIRMED vs UNKNOWN per component (CFM/NiFi, Strimzi/Kafka, CSA/Flink, EFM, MiNiFi Java), upstream fallbacks with image names.
## 10. Local knowledge base and local agent loops — components table (ingest → embed → store → MCP → Claude Code) with concrete tools/images, and the local-validator patterns found.
## 11. Load-bearing claims and their verification — table from verify.json: claim · source · votes per lens · verdict tag · consequence for the plan.
## 12. Open questions
Then Definition of done / When this ships / Resources. Tag every entry with the chapter(s) it feeds. Target ≥80 distinct source URLs. Confidence tags on every numeric claim.`,
  },
  {
    file: 'nvidia-dgx-spark-landscape.md', ws: 'A', issue: 232, model: 'opus', effort: 'high', target: '220-300',
    research: ['r02-playbooks-inference','r06-community','r07-github-examples','r08-x-posts','r04-playbooks-data-cluster'],
    precedent: ['the CURRENT nvidia-dgx-spark-landscape.md (expand it, keep what is right, fix what the research contradicts)', 'efm-nvidia-nano-research.md', 'research/gpu-optimization-plan.md (the VRAM sizing method)'],
    spec: `EXPAND in place. Keep the three-regime framing but rebuild the numbers from the research (with dates). Add: §MoE vs dense on a 273 GB/s box (active-parameter bytes per token; the @jmurillocode finding); §speculative decoding measured (before/after tok/s); §engine comparison table (llama.cpp / vLLM / SGLang / TRT-LLM / NIM / Atlas if sourced) with what each is for here; §leaderboards (spark-evals, dgx-spark-bench, Spark Arena) with the actual top rows; §the non-LLM tier — embedding, reranker, Whisper models for the RAG-stack migration with memory footprints; §128 GB co-hosting budget — a table: lead model resident + KV cache + k3d/CSO stack (NiFi ~4–6 GB JVM, Kafka 3 brokers, Flink JM/TM, EFM agent, Qdrant/TEI/Whisper) with headroom, and the statement that the ~100 B stretch model is a demo mode not a resident; §6 model lock — THREE candidates for the lead slot and THREE for the stretch, each with sourced tok/s, memory, context, license, and a Cloudera-alignment note (Nemotron family for Agent Studio parity), ending in a recommendation Steven can accept or change; dual-Spark note. Feeds ch01, ch04, ch06, ch07, ch22.`,
  },
  {
    file: 'nvidia-dgx-spark-runbook.md', ws: 'B', issue: 233, model: 'opus', effort: 'high', target: '260-350',
    research: ['r01-nvidia-docs','r03-playbooks-agents-dev','r05-k8s-on-spark','r02-playbooks-inference'],
    precedent: ['the CURRENT nvidia-dgx-spark-runbook.md (expand in place)', 'CLAUDE-CHECKIN.md — the NvidiaSpark-1 placeholder block and the NvidiaNano block as the template; the roster onboarding is three files: CLAUDE-CHECKIN.md, .claude/hooks/lib-device.sh ds_device_labels() case arm, agent/device-comms.md two tables', 'efm-nvidia-jetson-nano.md (the source-runbook shape with Resources + Appendix)', 'agent/incident-rules.md §Port-forwards (the firewall rule lesson)'],
    spec: `EXPAND in place into the full DEVICE runbook (not a serving runbook): §0 before it arrives (checklist incl. the three Phase-0 gates from the plan); §1 unbox + DGX OS first boot exactly per NVIDIA's guide (with the URL), record versions; §2 updates and recovery (what the System Update Guide says); §3 network — static IP, Tailscale (NVIDIA's playbook), the Windows-firewall-style lesson applied to whatever the Spark's firewall is, LAN exposure rules; §4 Docker + NVIDIA container runtime proven with a GPU container; §5 join the array — the three-file roster onboarding with the exact edits, gh label already exists, the EFM reachability check to WindowsDesktop (http://192.168.1.121:10090/efm/api) and Kafka bootstrap 192.168.1.121:31623 with the /etc/hosts lines the Jetson uses; §6 k3d with GPU — pointer to nvidia-dgx-spark-k3d-cso.md but the day-one smoke test lives here; §7 first serving endpoint (lead model per landscape §6; SGLang/vLLM prebuilt images from the research; then NIM variant) with curl checks from another device; §8 hardening (bind, auth, EarlyOOM, tunables); §9 DGX Dashboard + dgx-spark-prometheus + the fleet board; §10 what to record back (the checkin block fields). Every command block marked expected/verify. Close with Appendix — reusable command forms. Feeds ch02, ch03, ch08 smoke test.`,
  },
  {
    file: 'nvidia-dgx-spark-k3d-cso.md', ws: 'F', issue: 238, model: 'opus', effort: 'high', target: '280-380',
    research: ['r05-k8s-on-spark','r11-cso-arm64','r02-playbooks-inference','r04-playbooks-data-cluster'],
    precedent: ['flink-plan.md (§7 GPU Flink history: custom-flink-gpu:v5, x86_64) and /home/tunas/flink-gpu/Dockerfile + your-flink-deployment.yaml', 'flink-agents-cso-plan.md (#231: Flink Agents needs Flink ≥1.20.3, own FlinkDeployment, LLM backend via OpenAI endpoint)', '/home/tunas/ClouderaStreamingOperators/README.md and files/agent-install-operators.sh (canonical install order and chart versions: cert-manager → strimzi 1.6.0-b99 → csa 1.5.0-b275 → cfm 3.0.0-b126 → schema-registry/surveyor)', 'cso-operator-app-plan.md (the GPU services on WindowsDesktop: vllm :8000 Qwen2.5-3B, whisper :8001, TEI, qdrant; MODULES; scale-to-0 teardown)', 'completed/how-to-nifi-and-ai.md (StreamTovLLM / InvokeHTTP shapes)', 'CLAUDE-CHECKIN.md WindowsDesktop block (what runs there today and the port map)'],
    spec: `NEW doc. §1 what runs where — a table of every component: WindowsDesktop today / Spark target / migrates? / rung; §2 the aarch64 gate — per-component confirmed/unknown from research §9, the K check on the Mac, the fallback image per component (Apache NiFi arm64 image, Strimzi multi-arch, Flink arm64) and what is lost with each fallback (EFM/CFM features); §3 k3d with GPU — the CUDA node image build, cluster create flags, device plugin ≥ v0.17.4 install, GPU smoke pod, and the k3s-bare fallback as a one-page swap; §4 operator install ported from agent-install-operators.sh — the exact sequence with what changes (no minikube tunnel/image load; k3d ingress; registry login; licence secrets), namespaces cld-streaming/cfm-streaming; §5 resource budget inside 128 GB (align with landscape's co-hosting table); §6 NiFi → local LLM — the InvokeHTTP flow shape against the Spark endpoint, Parameter Context for the token, custom Python processor only where native can't; §7 Kafka on the box vs WindowsDesktop's Kafka — which topics live where, external listener shape; §8 Flink on GPU — arm64 rebuild of custom-flink-gpu (what changes in the Dockerfile for aarch64 wheels), podTemplate GPU request, Flink Agents deployment with the Spark as backend (#231 tie-in); §9 the cutover ladder — one rung per WindowsDesktop GPU service (vLLM :8000 [with the OpenClaw bridge caveat], Whisper :8001, TEI embeddings, trt-infer/classify daemon, the Streamers RAG base URL) each with: Spark equivalent, proof from a second device, switch mechanism (which URL/param changes where — the port-change fan-out lesson), rollback, go/no-go; §10 what stays on WindowsDesktop permanently. Feeds ch07–ch11.`,
  },
  {
    file: 'nvidia-dgx-spark-efm-agent.md', ws: 'G', issue: 239, model: 'opus', effort: 'high', target: '220-300',
    research: ['r03-playbooks-agents-dev','r07-github-examples','r11-cso-arm64','r01-nvidia-docs'],
    precedent: ['efm-nvidia-jetson-nano.md and completed/nvidianano-minifi-ops.md (Java agent install, SysV/systemd unit, class flow with three HandleHttp legs :8080/:8081/:8082 → local daemons)', '/home/tunas/EdgeFlowManager/ch19-efm-and-nvidia-jetson.md (the chapter the Spark chapters sit one tier above) and ch21-metrics-and-observability.md (flow-level Prometheus exporter :9936, fleet board rows)', 'efm-nvidia-nano-inference.md (resident daemon + thin front doors)', 'efm-metrics.md', 'agent/incident-rules.md §EFM agent deployment'],
    spec: `NEW doc. §1 the agent — MiNiFi Java (why Java over C++, per the fleet's 2026-08 cutovers), aarch64 JRE, install path, service unit shape, enrollment ONLY via generateCommand with a fresh agentIdentifier, class NvidiaSpark-1, heartbeat + the EFM lastSeen-is-not-live caveat; §2 the class flow v1 — HandleHttp front doors on the Spark for classify (vision model), transcribe (Whisper), reason (LLM chat/RAG), embed — each leg: listener port, target local endpoint (from the runbook), request/response shape, what the Jetson leg looked like and what changes; §3 out-of-box use cases — a table of 8–12 use cases (edge camera → Spark VLM, MicroFi sensor stream → Spark LLM summariser, Twitch chat → Spark classifier, AMOLED voice clip → Whisper → LLM → DisplayMessage, Sparkplug B anomaly scoring, doc ingestion into the local KB, NiFi-on-WindowsDesktop → Spark inference, Jetson → Spark escalation) each with the flow shape, which existing flow export it reuses (files/*.json names from the repo), and its chapter; §4 observability — the flow-level Prometheus exporter, dgx-spark-prometheus host exporter, DGX Dashboard, the fleet board row; §5 resources/assets EFM pushes (Python scripts, model configs) and the Resource Manager API precedent; §6 gallery entries this produces (Ch18-style cards); §7 what NOT to do. Feeds ch12, ch13, ch14.`,
  },
  {
    file: 'nvidia-dgx-spark-local-kb.md', ws: 'H', issue: 240, model: 'opus', effort: 'high', target: '220-300',
    research: ['r12-local-kb-mcp','r03-playbooks-agents-dev','r07-github-examples','r08-x-posts'],
    precedent: ['cso-operator-app-plan.md (Qdrant my-rag-collection 768-d, TEI nomic-embed-text-v1, the ingest flow)', 'blog/How To Install Cloudera Iceberg MCP Server.md (how an MCP server was wired into Claude on this fleet)', 'agent-to-agent.md and files/claw-claude.sh (OpenClaw headless mode; the local Qwen2.5-3B at :8000 that answers Telegram /bash)', 'CLAUDE.md and agent/workflow.md "Finding the pattern you need" (the ladder the KB must serve: skill → memory → root .md grep → sub-repo)', 'skills/README.md'],
    spec: `NEW doc. §1 what "local" means here — the three things Steven asked to keep local: execution, retrieval, agentic field validation — and the privacy boundary (what never leaves the LAN); §2 corpus — DesktopShare root .md + completed/ + blog/, EdgeFlowManager chapters, the nifi-and-ai skill, Cloudera docs subsets, NVIDIA playbooks, flow exports; chunking rules, refresh trigger (post-pull hook); §3 stack on the Spark — ingest → embed (model choice from landscape, arm64 image) → Qdrant (reuse of the existing collection convention or a new one) → MCP server (name the concrete server(s) found in research, config JSON for Claude Code's .mcp.json / claude mcp add) → what a query looks like from Claude Code; §4 the local validator loop — a local model reviewing a planned command/doc before Claude acts: the OpenClaw/NemoClaw/CLI-coding-agent playbook patterns, our claw-claude.sh precedent, what it checks (incident-rules, skill rules), how it is invoked (hook? MCP tool? sub-agent with OpenAI-compatible endpoint?), and its limits; §5 what moves off Anthropic tokens — a table of workloads (research fetch+extract, lint passes, doc RAG, log triage, EFM heartbeat checks) with the measurement plan (tokens before/after, latency); §6 rollout — phases tied to the plan's Phase 5; §7 what NOT to do. Feeds ch15, ch16, ch17.`,
  },
  {
    file: 'nvidia-dgx-spark-cloudera-aws.md', ws: 'I', issue: 241, model: 'opus', effort: 'high', target: '280-380',
    research: ['r09-cloudera-ai','r10-cloudera-aws','r02-playbooks-inference','r04-playbooks-data-cluster'],
    precedent: ['cloudera-iceberg-rest-catalog-aws-plan.md (the live CDP Public Cloud env srm-iceberg: cdp-tf-quickstarts, semi-private, weekly redeploy, daily startup, sandbox reaper)', 'blog/cloudera-ce-cm-evaluation.md (CE on AWS in one command; the 7 snags incl. amd64-only image; pause/resume cost control)', 'get-started-cloudera-ai-workbench.md (the live Workbench)', 'spark-versus-cso-1.md (Apache Spark vs CSO framing — use the naming rule)', 'nvidia-dgx-spark-cloudera-demos.md current version'],
    spec: `NEW doc. §1 the two Cloudera-on-AWS shapes side by side (Base/CE on EC2 vs Public Cloud) — what each is, what we already run, cost/time to stand up, what the Spark is to each; §2 CDP Base / Community Edition on AWS — deploy path (CE repo), the amd64-only fact and why it doesn't matter (Base runs on AWS, the Spark feeds it), inbound paths for Spark-hosted NiFi (Site-to-Site to Base NiFi, Kafka external listeners), RAPIDS for Apache Spark on Base GPU nodes, a concrete first integration; §3 CDP Public Cloud on AWS — the srm-iceberg environment as the target, DataFlow inbound, Data Hub Kafka, Iceberg REST catalog from Spark-hosted NiFi, and Cloudera AI: Workbench → AI Registry → AI Inference (NIM) on GPU instance types (which ones, quotas), Agent Studio + Nemotron; API shape (auth header, OpenAI-compatible path) with the base-URL swap spelled out; §4 NIM on the Spark for parity — which NIM, how it runs on GB10 (from the playbook), the identical request against both; §5 "same code, two backends" formalized — one Python client, one NiFi flow, one Flink Agents job, each with the exact config delta; §6 out-of-box integration catalogue — a table of 8–10 integrations (each: Spark side, AWS side, data path, demo value, chapter); §7 stand-up runbooks pointers and cost control (pause/resume, reaper); §8 what NOT to do. Feeds ch05, ch18, ch19, ch20.`,
  },
  {
    file: 'nvidia-dgx-spark-cloudera-demos.md', ws: 'C', issue: 234, model: 'opus', effort: 'medium', target: '160-220',
    research: ['r09-cloudera-ai','r03-playbooks-agents-dev','r02-playbooks-inference'],
    precedent: ['the CURRENT nvidia-dgx-spark-cloudera-demos.md (expand in place; keep the four demos, renumber if needed)', 'cso-operator-app-plan.md', 'flink-agents-cso-plan.md', 'completed/how-to-nifi-and-ai.md'],
    spec: `EXPAND in place. Keep reuse-first. Re-map the existing four demos onto the new docs (F/G/H/I) and add: Demo 5 NiFi on the Spark's own k3d → local LLM → Kafka on the box (F); Demo 6 Flink Agents on the Spark with the local model (F/#231); Demo 7 Agent Studio + Nemotron parity (desk NIM vs Cloudera AI Inference) (I); Demo 8 Spark-hosted NiFi → CDP Public Cloud Iceberg/Kafka (I); Demo 9 edge → desk → cloud ladder with the EFM agent (G); Demo 10 local knowledge base answering a Claude Code question about our own flows (H). For each: shows / reuses (exact artifact) / bridge / backing model (from landscape §6 candidates) / chapter. §sequencing with gates. Feeds ch21.`,
  },
]

phase('Author')
const AUTH_SCHEMA = { type: 'object', properties: { file: { type: 'string' }, lines: { type: 'number' }, source_urls_count: { type: 'number' }, chapters_fed: { type: 'array', items: { type: 'string' } }, open_questions: { type: 'array', items: { type: 'string' } } }, required: ['file','lines','source_urls_count','chapters_fed','open_questions'] }
const LINT_SCHEMA = { type: 'object', properties: { file: { type: 'string' }, lines: { type: 'number' }, source_urls_count: { type: 'number' }, fixed: { type: 'array', items: { type: 'string' } }, remaining: { type: 'array', items: { type: 'string' } }, verdict: { type: 'string', enum: ['pass','pass-with-notes','fail'] } }, required: ['file','lines','source_urls_count','fixed','remaining','verdict'] }

const out = await pipeline(DOCS,
  d => agent(`${COMMON}\nTARGET FILE: ${REPO}/${d.file} — work-stream ${d.ws}, issue #${d.issue} (${I}${d.issue}). Target length ${d.target} lines.\nRESEARCH JSON TO READ (under ${R}): ${d.research.join(', ')} — plus verify.json and critic.json.\nPRECEDENT TO READ: ${d.precedent.join(' | ')}\n\nSPEC:\n${d.spec}`,
    { label: `author:${d.file}`, phase: 'Author', schema: AUTH_SCHEMA, model: d.model, effort: d.effort }),
  (a, d) => agent(`You are the LINT pass for ${REPO}/${d.file} (work-stream ${d.ws}, #${d.issue}). Read ${REPO}/agent/writing-style.md, then the file. Check and FIX IN PLACE with Edit (minimal edits, never rewrite sections): (1) opens with '# Title' then a '> **Status (2026-08-24):**' blockquote; closes with '## Definition of done', '## When this ships', '## Resources' in that order; (2) every number/version/tok-s/GB/date/product-status claim has an inline source link — if one is missing, look it up in the research JSON under ${R} (any *.json) and add the link; if not found, change the sentence to say "unverified" — never delete the claim silently; (3) naming: "DGX Spark" for the box, "Apache Spark" for the engine, no bare ambiguous "Spark", device "NvidiaSpark-1"; (4) LLM tells (delve, leverage, "in the fast-paced world", "it's worth noting", "certainly", summary sentence-endings, em-dash emphasis where a period works) removed; no '## Introduction'/'## Conclusion' headers; first person present tense; (5) every backticked .md filename that refers to a repo doc actually exists under ${REPO} (check with ls/Read; the nine nvidia-dgx-spark-*.md, the tracker, files/nvidia-spark-guide/ and chNN stubs are all valid); fix wrong names; (6) command blocks have a language tag; pre-arrival commands carry an "expected — verify on the box" comment; (7) the literal phrases "git commit"/"git push" do not appear. Do not run git/gh/kubectl/curl. Do not call AskUserQuestion. Count lines and distinct http(s) URLs after fixing. Return the structured output.`,
    { label: `lint:${d.file}`, phase: 'Lint', schema: LINT_SCHEMA, model: 'sonnet', effort: 'medium' })
    .then(l => ({ file: d.file, author: a, lint: l })))

return out.filter(Boolean).map(o => ({ file: o.file, lines: o.lint ? o.lint.lines : (o.author ? o.author.lines : null), urls: o.lint ? o.lint.source_urls_count : null, verdict: o.lint ? o.lint.verdict : 'no-lint', remaining: o.lint ? o.lint.remaining : [], open_questions: o.author ? o.author.open_questions : [] }))
