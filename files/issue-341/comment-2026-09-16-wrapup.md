**Delivered from the DGX Spark: Ch18 field-validated end to end. Commit [dee8acd](https://github.com/cldr-steven-matison/DesktopShare/commit/dee8acd).**

**Proven**
- The box drives the CE deploy natively (arm64 EE, #345); the aarch64 gating question is closed.
- Reverse tunnel, one per NiFi worker through the gateway as a jump (`ssh -N -R 127.0.0.1:8000:127.0.0.1:8000`): `nvidia/Qwen3.6-35B-A3B-NVFP4` answers on `127.0.0.1:8000` from all four workers. The gateway sshd has `GatewayPorts no` and NiFi does not run there, so the per-worker shape is the one that works without an sshd or SG change. [tunnel-probe-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/tunnel-probe-2026-09-16.txt).
- `Ch18LlmBridge` PG on CE NiFi, built through Knox by [build-flow.py](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/build-flow.py): GenerateFlowFile (primary node, 60 s) → InvokeHTTP POST `/v1/chat/completions` → PublishKafka over SASL_SSL/GSSAPI, Retry self-looped with a 10 min expiry, failures logged. Three chat completions published to `ch18-llm-responses` and read back by the console consumer on sdx-01: [kafka-readback-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/kafka-readback-2026-09-16.txt), build transcript [build-2026-09-16.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/build-2026-09-16.txt), export [Ch18LlmBridge.flow.json](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/Ch18LlmBridge.flow.json) (sensitive value exports as null).
- Kerberized Kafka from a CM-managed NiFi without copying a secret into the flow: `KerberosKeytabUserService` with `nifi/${hostname(true)}@CLDR.INTERNAL` and `${CONF_DIR}/nifi.keytab` (the CSD exports `CONF_DIR` to the JVM, both properties take environment EL), `StandardSSLContextService` on the AutoTLS global truststore with the password as a sensitive Parameter Context value, `Kafka3ConnectionService` on the four brokers at 9093. Ranger grant for `nifi`/`admin` on `ch18-*`: [ranger-kafka-grant.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/ranger-kafka-grant.sh) (you ran it), result [ranger-kafka-grant-result.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-341/ranger-kafka-grant-result.txt).

**Two build traps recorded in ch18:** the CFM 4.10 `Kafka3ConnectionService` needs the `SSLContextService` interface (the PEM provider fails the cast at enable), and an InvokeHTTP property name that does not exist becomes a request header, which OkHttp rejects for the space in it.

**Surfaces:** [ch18-cdp-base-ce-on-aws.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/nvidia-spark-guide/ch18-cdp-base-ce-on-aws.md) written as built (lint: em-dash 2.2/k from the title and Next-link conventions only, proof 2.2/k, colon 10.0/k); [nvidia-dgx-spark-cloudera-aws.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/nvidia-dgx-spark-cloudera-aws.md) §2.2 corollary corrected, §2.3 tunnel block and §2.5 as built; tracker row 18 ✅ + status paragraph + count ([Complete Developer Guide for Nvidia Spark with Cloudera.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/Complete%20Developer%20Guide%20for%20Nvidia%20Spark%20with%20Cloudera.md)); README row unchanged (no status column).

**Not done / scope notes.** RAPIDS on GPU nodes stays deferred per the body. No UI screenshots (KnoxSSO needs the internal name); API transcripts stand in. Artifacts are in `files/issue-341/` (the body says `files/issue-340/`). The PG is stopped with one FlowFile queued from the last cycle; the four tunnels are still up from this session and die with it. The Ozone fix, sizing and the services stopped for the validation are on #345.
