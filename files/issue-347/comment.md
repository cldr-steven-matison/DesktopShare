**Done from `spark-dd06` — all four proofs pass, every goes01 subnet reachable.** Commit [e1284fa](https://github.com/cldr-steven-matison/DesktopShare/commit/e1284fa).

**Step 1 — VPN.** The client was already on the box: `globalprotect-openconnect` 2.6.5 (`gpclient`), portal `cloudera.gpcloudservice.com`, Okta SAML in Chrome. Wrapped as [vpn-connect.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/vpn-connect.sh) (refuses to start a second connect — a parked one holds `gpauth` on the SAML step) and [vpn-check.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/vpn-check.sh). It is a **full tunnel**: `default dev tun0`, `10.80.156.1 dev tun0`, TCP connect to `console` and `cdf` OK. Two Linux-only findings on the way:
- **Tailscale vs the VPN for `/etc/resolv.conf`.** gpclient wrote the corp resolvers; `tailscaled` overwrote them 30 s later ("resolv.conf was trampled"), and once the full tunnel was up Tailscale's `100.100.100.100` resolver answered nothing at all. Fixed with `sudo tailscale set --accept-dns=false` (MagicDNS names off on this box; peers by IP, nothing here used them). `github.infra.cloudera.com` still needs the corp resolver (`dig @172.17.64.15`) — not installed; the `goes-certs` clone was shipped from the Mac instead.
- **The AT&T gateway isolates wireless clients**: the MacBook on the same SSID could ARP the box but not ping/SSH it (zero packets reached ufw, port 22 open); WindowsDesktop (wired) reaches it fine. Recorded in the device block, with the two Wi-Fi profiles (`ATTyjuHfEi 1` → `192.168.1.203`, `STARLINK` → `.144`) and the switch command.

**Step 2 — CA chain.** [goes-certs-import-linux.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/goes-certs-import-linux.sh): 12 roots from `certs/goes01_awc` → `/usr/local/share/ca-certificates/goes01/` → `update-ca-certificates`. `console`, `cdf`, `csa`, `cai` verify with `ssl_verify=0`, no `-k`. The `goes01-cle-int-cluster` hosts are not in the chain (same as on the Mac).

**Step 3 — token.** [awc-env.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/awc-env.sh) is the Linux port (`awc_api` on `/api/v0/console`, `cdf_api`, `ssb_api`, `trino_q`), [awc-cookie.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/awc-cookie.sh) reads Firefox's cookie store (Chrome on Linux is keyring-encrypted → [awc-creds-set.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/awc-creds-set.sh), hidden paste). Console host recorded: `console.goes01-se-goes.demos.cloudera-labs.com`. **The token used tonight was pasted into the session** — treat it as exposed; it is session-scoped, so a fresh Knox login invalidates it. Tonight's copy lives only in the job scratchpad, not in `~/.awc.creds` — run `awc-cookie.sh`/`awc-creds-set.sh` once for the durable file.

**Step 4 — the four proofs** ([awc-check.sh](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/awc-check.sh), record [awc-check-2026-09-15T2320Z.txt](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-347/awc-check-2026-09-15T2320Z.txt)):
1. `awc_api /experiences` → **17 experiences**, all `deployed`.
2. `cdf_api /cdf/api/v1/deployments` → `totalElements: 1` (`rsingh1`). GET works with the JWT cookie alone; `XSRF-TOKEN` comes back as a `Set-Cookie` on that first GET, so `cdf_api` self-fetches it.
3. SSB `/api/v1/` → structured JSON `500 No endpoint` (auth passing) — **as a `Cookie`**; `Bearer` gets a `302` to websso. The doc said Bearer; corrected.
4. Trino `/v1/info` → `cloudera-0.479.1`, `SELECT 1` → `[1]`, **`SHOW CATALOGS` → `hive`, `iceberg`, `system`**.

Live-state drift found while doing it (doc updated, dated): the **Basic Lakehouse Engine is gone** (`goes01-cle-t-536b9e` doesn't resolve); only the Integrated engine remains, its coordinator answers the token's user without the Ranger denial #284 hit, and the catalogs are wired — the "no data catalog" problem is solved platform-side. The standalone Object Store experience is gone too; Ozone gateways now sit inside CLE-Integrated (`10.80.158.220`) and CDE-UDF (`10.80.154.41`).

**Subnet map from the box** (all TCP OK): console `10.80.156.1`, cdf `.155.216`, csa `.155.227`, cle-int `.158.220`, **csm `.133.150` (`:8443` + Surveyor `:443` — the Mac cannot reach this)**, both Ozone S3 gateways, cai `.186.131`, cdx `.140.64`.

**Step 5 — sweep:** [CLAUDE-CHECKIN.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/CLAUDE-CHECKIN.md) NvidiaSpark-1 block (VPN/CA/creds bullet, Network rewritten for the two Wi-Fi networks, Tailscale DNS off), [cloudera-anywhere-getting-started.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/cloudera-anywhere-getting-started.md) ("From Linux" section + the corrections above), [part3-cloudera-plan.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/files/issue-346/part3-cloudera-plan.md) goes01 row, [nvidia-dgx-spark-cloudera-awc.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/nvidia-dgx-spark-cloudera-awc.md) §2 + open question, [nvidia-dgx-spark-cloudera-aws.md](https://github.com/cldr-steven-matison/DesktopShare/blob/main/nvidia-dgx-spark-cloudera-aws.md) §4 gate row. #343's reachability gate can now run from the box.

Not done: a Chrome cookie decryptor for Linux (Firefox path + paste instead), and installing the corp resolvers for `github.infra` (dig-by-address works; a proper split-DNS needs `systemd-resolved`'s stub, which DGX OS disables).
